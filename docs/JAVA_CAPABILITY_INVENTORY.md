# Java Capability Inventory

This is the frozen-baseline Capability / Entry Point inventory for Migration Baseline v1. It is the exhaustive trace from each named user-reachable Java Capability in research 01–07 to exactly one Parity Item, an explicit split list of resulting items, an owner-routed mapping, or an explicit exclusion. Dispositions come from Tickets 08–14 and later decision tickets. This file does not invent Parity IDs, does not assign acceptance status, and does not replace [`PARITY_MATRIX.md`](PARITY_MATRIX.md) or [`MIGRATION_PLAN.md`](MIGRATION_PLAN.md).

## Authority

| Field | Value |
| --- | --- |
| Java Migration Baseline v1 | [`7b4027531c2b26062d0bfc27a040cc550cfbea4d`](https://github.com/wimi321/lizzieyzy-next/commit/7b4027531c2b26062d0bfc27a040cc550cfbea4d) |
| Frozen Java worktree | `/home/dev/dev/weiqi/worktrees/lizzieyzy-next/java-baseline-v1` |
| Next inventory baseline | `18c6d189b8b01069975c4c40ead63a010249cb8c` |
| Next worktree | `/home/dev/dev/weiqi/worktrees/lizzieyzy-next-tauri/migration-coverage-audit` |
| Language | [`CONTEXT.md`](../CONTEXT.md): Capability, Entry Point, Disposition, Parity Item, Successor Item |
| Census | [research 01–07](../.scratch/migration-coverage-audit/research/); independent recomputes [baseline-v1 research 01–07](../.scratch/migration-baseline-v1-capability-audit/research/) |
| Corpus reconciliation | [Ticket 08](../.scratch/migration-baseline-v1-capability-audit/issues/08-reconcile-inventory-routing.md) / [research 08](../.scratch/migration-baseline-v1-capability-audit/research/08-reconcile-inventory-routing.md) |
| Dispositions | [Tickets 08–14](../.scratch/migration-coverage-audit/issues/) plus remainder closures [17–28](../.scratch/migration-coverage-audit/issues/) |

Evidence limits:

- A census row is accepted only from those two frozen commits. Later Java `main` does not expand this inventory in place.
- Completeness is a static source census of registered, user-reachable surfaces plus targeted runtime notes where the research marked `需定向运行核验`. Those runtime checks were not executed for this document.
- Source citations below are the frozen Java evidence recorded in research 01–07. They are not Next implementation templates.
- This inventory records baseline defaults, persistence, and failure/recovery. It does not claim that Next already implements them.
- Repository evidence, release-environment evidence, and Installed Live Evidence remain distinct classes (Ticket 14). None is inferred from another.
- Existing Accepted Parity Item IDs, scopes, and evidence stay intact. Materially new behavior is a Successor Item named by Tickets 08–14 or later remainder tickets.

## How to read a row

Every Frozen ID from research 01–07 appears **once**. Ticket 08 reconstructed **135** unique Java Capabilities from the seven domain tables; reference 139. The four Frozen IDs that are not Capabilities (`CAP-04-ANA-05`, `CAP-04-ANA-14`, `REL-C12`, `REL-C13`) stay in their tables and in the completeness index. Ticket 01 routing keys that only point at domains 02–07 are not extra Capabilities; the owning research file already names them.

| Column | Meaning |
| --- | --- |
| Frozen ID / name | Census identifier and observable user goal |
| Entry Points | Registered, user-reachable controls. Constructed-but-unadded widgets are not Entry Points |
| Default / persistence | Frozen Java default and where the value lives |
| Failure / recovery | Frozen Java failure presentation and recovery |
| Frozen evidence | Source citations for the frozen Java Capability |
| Runtime-check | Domains 01–07 record this as their own column. Values: `Not required`, or a named dynamic-visibility / default / persistence / failure fact not recoverable from frozen source. Packaged/installed association live evidence is a `REL-04` link, not a Domain 01/02/03/04/05/07 Java runtime check. Provider-live, sidecar-live, credential, and `GAME-10` / `READ-02` sidecar-confirmation evidence are Matrix / `REL-*` classes, not Domain 05/06 Java runtime checks. |
| Disposition | Ticket 08–14 product decision, plus remainder closures in Tickets 17–28 |
| Mapping | Exactly one supported Parity Item, or an explicit split / owner-route / exclusion. No invented IDs |

Split rule: when a later ticket deliberately split or merged a census Capability, this file keeps the original census row and lists every resulting item or exclusion in that row.

Owner-route rule: Ticket 09 moved several settings to domains 03–07. If Tickets 10–14 or remainder Tickets 17–28 later named a stable Parity ID or exclusion, that record is the mapping. Tickets 17–28 close every Ticket 16 gap; no owner-routed row may be silently absorbed into `ANA-04`, `UI-01`, or `PREF-01`.

## Defaults, persistence, and failure ownership

| Concern | Owner | Frozen / decided contract |
| --- | --- | --- |
| Durable application preferences | `PREF-01` | Native preference file. Missing file loads defaults. Unreadable file is isolated, defaults load, recovery is reported. An explicit setting becomes durable only after an atomic write succeeds; failure preserves the last durable value and reports the error. Java `config.txt` is not imported. |
| Window geometry | `WINDOW-01` | First launch uses the OS/Next default. Persist valid position/size; auto-reset only geometry that is invalid for current displays. Explicit reset changes window geometry only. |
| Workspace proportions | `LAYOUT-01`, `LAYOUT-02`, `LAYOUT-03` | Direct divider manipulation; atomic save after debounce with visible unsaved state on failure; narrow restore of panel sizes and board proportion only. |
| Rail visibility | `LAYOUT-04` | Left and right rails visible by default; independent collapse persists through `PREF-01`. |
| Appearance | `APPEAR-01` | Classic default, High Contrast alternate, persisted through `PREF-01`. System DPI; no app font slider. |
| Educational tips | `GUIDE-01` | Persist dismissals only for **named** Educational Tips; Reset Guidance re-enables those tips. Dirty-state Safety Confirmations are never permanently dismissible. Ticket 21 Defers `GUIDE-01` until a later disposition names a producer whose owner is in a numbered phase. `SET-HINT-AUTOANALYZE` is excluded with abandoned `CAP-04-ANA-08`. `ANA-06` and Ticket 25 notices do not auto-start this item. |
| Current-game replacement | `SGF-07` | Parse/validate before commit. Cancelled or failed Save / Discard / Cancel preserves the complete current game. |
| Session recovery | `APP-04` | Recovers SGF tree, personal comments, `NodePath`, source path, and dirty state after an unclean exit. Never restores engines, analysis jobs, Match Sessions, or provider sessions. Normal-launch restore defaults off. |
| Graceful shutdown | `APP-03` | Dirty game uses Save / Discard / Cancel. Failed or cancelled save aborts exit. Teardown timeout names the stuck resource and offers Retry or contextual Exit anyway. |
| Engine run / jobs | `ENG-02`–`ENG-07`, `ANA-03` | Runs, capability snapshots, and jobs do not persist across application restart. Typed failure; no automatic restart. |
| Match sessions | `GAME-01`, `GAME-05` | Live turn/pause/job/budget state does not persist. Restart never restores a Match Session. |
| Provider / sidecar | `PROV-01`–`PROV-07`, `READ-02` | Preview never mutates the current game. Ongoing sync preserves last-good board on failure; stop leaves an editable game. Timeouts: Yike 10s; Fox/Tencent 20s connect / 25s read. At most three retries after the initial idempotent read; provider writes are never automatically retried. Remote-provider HTTP(S)/WebSocket(S) follows Provider Network Policy; browser-owned traffic, local readboard, and inbound WebBoard are excluded. Only Deferred `PROV-07` may persist a provider-issued user secret, and only in the System Credential Store; proxy secrets never persist in Next. |
| Updates | `REL-03`, `REL-06`–`REL-08` | Discovery failures do not mutate the install. Windows apply backup/rollback plus result journal. macOS/Linux never overwrite in place. OS/system proxy only; no application-specific update proxy. |
| Semantic settings | Owner domain | Lifecycle-affecting engine, analysis, match, provider, and review settings travel with their behavior owners. They are not a catch-all settings phase. `PREF-01` is the durable-preference mechanism, not the semantic owner. |

Java baseline persistence files (`config.txt`, `persist`, `save/save`) remain Java-only evidence. Next persistence is the native preference / profile / cache / app-data contracts named by the mapped items.

---

## Domain 01 — Application shell, startup, exit, and interaction

Independent census: [research 01](../.scratch/migration-baseline-v1-capability-audit/research/01-application-shell-interaction.md) against Java `7b4027531c2b26062d0bfc27a040cc550cfbea4d`. Prior coverage-audit census: [research 01](../.scratch/migration-coverage-audit/research/01-application-shell-entry-points.md). Dispositions: [Ticket 08](../.scratch/migration-coverage-audit/issues/08-decide-application-shell-dispositions.md).

**Computed Domain 01 Capabilities: 13.** Reference 13. Extra/missing rows: none. The count was computed from registered, user-reachable surfaces; 13 was compared afterward and was not a stop condition.

Domain 01 owns global Entry Point routing and process/chrome lifecycle only. Runtime-check is this domain's dedicated column.

| Frozen ID | Name | User-reachable Entry Points | Default / persistence | Failure / recovery | Frozen evidence | Runtime-check | Disposition | Mapping |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `SHELL-01` | Launch the desktop process and show the main window | OS executable / `java … featurecat.lizzie.Lizzie`; installer launch (07) | `appName=LizzieYzy Next`; `lizzieVersion=2.5.3`; `nextVersion` from `-Dlizzie.next.version` / `LIZZIE_NEXT_VERSION` else `next-dev`. Config load; first-run may write a profile. | Hostname lookup timeout 500ms never treats unknown host as machine change. Logging bootstrap failure prints stderr and continues. Engine-manager construct failure → empty manager. Profile save failure → `startupProfileSaveFailed`. | `Lizzie.java:320-460,967-1045` | Not required. Installed association/launch evidence is `REL-04` live, not a Domain 01 Java runtime check. | Equivalent migration (08) | `UI-01` chrome; `UI-04` no-engine launch; `REL-04` installed launch. No new shell item. |
| `SHELL-02` | Choose whether to autoload an engine, start empty, or show the engine picker | Startup only. Flags that *set* autoload belong to 02/04. | Missing keys: all autoload flags false (manual). New bundled KataGo profile writes `autoload-default=true`. Persist `uiConfig` `autoload-default/last/empty`, `last-engine`, `default-engine`. | `shouldOfferEngineRepair` when no configured engine and not autoload-empty. | `Lizzie.java:415-454,1129-1133`; `Config.java:762-790` | LoadEngine dialog visibility and empty-engine first paint (dynamic visibility) | Next redesign, domain 04 (08, 11) | `ENG-06` zero-or-one Autoload Default (first use unmarked, starts with no engine). No Java modal picker. Empty/failed start is `ENG-02` no-engine run. |
| `SHELL-03` | Open a path passed as the single CLI argument | `args[0]` when it is not `"read"` and `mainArgs.length == 1` | N/A if no argv. Recent files via 03. | `loadFile` errors owned by 03. Extra argv after the first is ignored. | `Lizzie.java:1012-1017` | Not required. Warm single-instance activation has no Java `OpenFilesHandler` / IPC (Next redesign under `APP-01`). Installed Open With is `REL-04` live. | Next redesign (08) | `APP-01` Native File Activation for `.sgf` and `.gib` (cold launch / warm focus). Replacement uses `SGF-07`. |
| `SHELL-04` | `read` launch mode (hide status and engine menus) | argv exactly `read` | `readMode=false`. Mode is not persisted. | N/A | `Lizzie.java:432-437`; `Menu.java:6547-6550` | Remaining analysis UI visibility under `readMode` (dynamic visibility) | Abandoned (08) | Exclusion — Abandoned product capability. No-engine review remains `UI-04`. |
| `SHELL-05` | Reload last auto-saved SGF when no CLI file | Startup if `config.autoResume` | `resume-previous-game` default **false**. Files `save/autoGame1.sgf` else `autoGame2.sgf`. File › Resume is commented and is not an Entry Point. | Missing file → skip. | `Lizzie.java:1018-1020`; `LizzieFrame.java:4523-4534` | Dirty autosave index shuffle when `save-auto-game-index2 == -5` (persistence) | Next redesign (08) | `APP-04` Current-game Session Recovery. Prompt after unclean exit; default-off restore-at-launch preference. |
| `SHELL-06` | First-run engine setup and hostname-change persist wipe | Startup when `firstTimeLoad` or resolved hostname differs | New profile / `firstTimeLoad`. Persist `host-name`, `first-time-load`. | Save IOException → `startupProfileSaveFailed` and repair chip. Unknown host never treated as machine change. | `Lizzie.java:374-386,518-627,894-964` | Not required | Split redesign / abandon (08, 09, 11, 14) | First-run engine/profile bootstrap → `ENG-06` / `REL-05` / domain 04 failure presentation. Hostname-based deletion **Abandoned**. Invalid window geometry → `WINDOW-01`. Does not auto-open FirstUse (`SET-FIRST-USE`). |
| `SHELL-07` | Overlay chip to repair a failed/missing engine at launch | `engineStartupStatusButton` click when snapshot `isActionable` | Hidden until actionable. Not persisted. | Click opens KataGo autosetup; further repair is 04. | `LizzieFrame.java:1898-1945` | `engineStartupStatusButton` visibility and copy when snapshot `isActionable` (dynamic visibility) | Next redesign, domain 04 (08, 11) | Fold into Engine Switcher / Engine Settings lifecycle and `ENG-07`. No duplicate shell identity. |
| `SHELL-08` | Persist, stop engines/sidecars, then exit 0 | File › Exit; window close (`WINDOW_CLOSING`) | `auto-save-exit` default **true**. Persist file + config; optional autosave SGF. | Persist/save errors show modal with path; resource failures print stack; process still exits 0. Java graceful path does not confirm dirty documents. `confirm-exit` is written in default config and never read by `shutdown()` — not a Capability. | `Lizzie.java:1428-1516`; `LizzieFrame.java:1820-1825`; `Menu.java:443-454` | Not required | Next redesign (08) | `APP-03` Safe Graceful Shutdown. Dirty game: Save / Discard / Cancel. Failed or cancelled save aborts exit. Timeout names the stuck resource; Retry or contextual Exit anyway. |
| `SHELL-09` | Exit immediately without persist | File › Force Exit | N/A. No persist. | Unsaved work lost. | `Menu.java:429-441` | Not required | Abandoned (08) | Exclusion — Abandoned product capability. Contextual timeout fallback lives only inside `APP-03` after graceful teardown fails. |
| `SHELL-10` | Present `Menu` as custom strip or native bar | Automatic at frame construct; `-Dlizzie.menu.presentation` | Default `auto` (Wayland Linux → native; else custom). Process property only. | N/A | `MenuPresentationMode.java:32-48`; `LizzieFrame.java:861-862,1270,1974-1976` | Native vs custom menu-strip focus and presentation (dynamic visibility) | Swing-only implementation (08) | Exclusion — Swing/implementation-only. Next HTML chrome is `UI-01`; Java menu-host mechanics are not a migration claim. |
| `SHELL-11` | Route focused main-board keys/mouse/wheel to Capabilities | `mainPanel` `Input`; independent-board / subboard inputs | Main board focus after start. Not persisted. | Unregistered keys are no-ops. Board `Input` is registered on `mainPanel`; comment editor and GTP console are separate focus owners (`LizzieFrame.java:8718,9905`), so they do not receive those key events. | `LizzieFrame.java:1889-1891,2859-2896,8718,9905`; `Input.java:12-880` | Not required | Next redesign (08) | `APP-05` Shortcut Registry and Reference. Does **not** expand accepted `UI-05`. Per-action semantics stay with owner domains. |
| `SHELL-12` | Drop files on the board to open or batch-analyze | OS drag-drop onto `mainPanel` | N/A | Exception print, return false. One file `loadFile`; multiple `isBatchAna` + `StartAnaDialog`. | `LizzieFrame.java:1277-1348` | `DataFlavor.javaFileListFlavor` `toString()` split parse (failure / non-mutation of drop) | Next redesign (08) | `APP-02` Window file-drop dispatch. One `.sgf`/`.gib` follows `APP-01`/`SGF-07`. Multiple files → explicit `ANA-07` batch intake (04); never silently replace the current game. |
| `SHELL-13` | Show shortcut cheat-sheet while X is held | `VK_X` press/release in `Input` | Off. Not persisted. | N/A | `Input.java:583-611,828-833`; `LizzieFrame.commands.*`. | Not required | Next redesign (08) | `APP-05` searchable Shortcut Reference from Help or `?`. Input controls do not surrender typed `?`. |

---

## Domain 02 — Settings, layout, and persistence

Independent census: [research 02](../.scratch/migration-baseline-v1-capability-audit/research/02-settings-layout-persistence.md) against Java `7b4027531c2b26062d0bfc27a040cc550cfbea4d`. Prior coverage-audit census: [research 02](../.scratch/migration-coverage-audit/research/02-settings-layout-persistence.md). Dispositions: [Ticket 09](../.scratch/migration-coverage-audit/issues/09-decide-settings-layout-dispositions.md), with later owner IDs from Tickets 10–14 and 17–21. Ticket 21 Defers `GUIDE-01` and excludes `SET-HINT-AUTOANALYZE`. Engine autoload, last-engine, analysis reuse/preload/auto-quit, and Lizzie cache are **04 Entry Points only**.

**Computed Domain 02 Capabilities: 35.** Reference 35. Extra/missing rows: none. The count was computed from registered, user-reachable surfaces; 35 was compared afterward and was not a stop condition.

Shared Java contracts (baseline evidence, not Next import): **SC-01** `config.txt` load/save/merge (`Config.java` 1529–1571, 3532–5339); **SC-02** `persist` geometry (`3017–3408`); **SC-03** shutdown persist-then-save with modal path on failure (`Lizzie.java` 1428–1470). Immediate `config.save()` here: FirstUse Confirm, Settings language `selectLanguage`, Help clear-personal-data, Appearance `persistUiSettings`, first-run `finalizeAutomaticFirstRunSetup`. Runtime-check is this domain's dedicated column.

| Frozen ID | Name | User-reachable Entry Points | Default / persistence | Failure / recovery | Frozen evidence | Runtime-check | Disposition | Mapping |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `SET-RESET-WINDOW-POS` | Delete persisted window/panel geometry and ask to restart | View `Menu.deletePersistFile` | Destructive. Deletes `persist`. Does not rewrite theme/language/engine in `config.txt`. Also calls `resetAllHints`. | After this action, shutdown persist write is skipped until restart. Dialog always shown when `showMsg=true`. | Source: `Menu.java:660-670`; `Config.deletePersist` 3469–3485. English label `Menu.deletePersistFile=Reset frame location` (`DisplayStrings.properties:848`). | Not required | Next redesign (09) | `WINDOW-01`. Automatic recovery only when saved geometry is invalid for current displays. |
| `SET-RESTORE-PANEL-SIZES` | Restore splitter / board-proportion sizes | View → 面板 `restoreDefaultPanelSizes` | N/A (restore action). Distinct from window reset. In-memory only: leftover-left / comment-height / variation-graph shares nulled; `BoardPositionProportion` reset to `InFrameLayout.DEFAULT_BOARD_POSITION_PROPORTION`; persist typo `board-postion-propotion` written. Window bounds unchanged. No immediate `persist()`/`save()`. Shutdown `Config.persist` 3087–3100 rewrites live shares (null → remove). | Not a persist write. Later SC-02 uses live field values. | Source: `Menu.java:1361-1369`; `LizzieFrame.restoreDefaultPanelSizes` 6773–6786. | Not required | Next redesign (09) | `LAYOUT-03` narrow restore of panel sizes and board proportions only. |
| `SET-RESET-HINTS` | Re-enable a subset of one-time hint flags | Not a menu. Callers: `SET-RESET-WINDOW-POS`; `Lizzie.main` on firstTimeLoad / hostname change | Four flags load-default true. Does **not** reset `showNewBoardHint`. Writes ui keys; `resetAllHints` itself does not `save()`. Hostname/first-run then `finalizeAutomaticFirstRunSetup` → `config.save()` (`Lizzie.java:953-964`). | No dedicated save; relies on later SC-01/SC-03 except the first-run path. | Source: `Lizzie.resetAllHints` 1518–1527; `Lizzie.java:953-964`. | Not required | Next redesign (09, 21) | `GUIDE-01` Reset Guidance (Deferred, Ticket 21) for named Educational Tips only; dedicated action, not `WINDOW-01`. `firstLoadKataGo` and Abandoned `SET-HINT-COMMENT-CTRL` are not Educational Tips. |
| `SET-FIRST-LAUNCH` | Finish first-run profile setup without the initialize wizard | Startup when `firstTimeLoad` or hostname differs | `first-time-load` load fallback true. Persist `first-time-load`, `host-name`. Host change wipes persist (`deletePersist(false)`). | Save IOException → repair chip. Hostname lookup 500ms never fakes a machine change. | Source: `Lizzie.java:374-386,894-965`; `Config.applyFirstLaunchDefaults` 3422–3434. | Not required | Abandoned / absorbed (09) | Hostname-triggered deletion **Abandoned**. First launch uses defaults of `PREF-01`, `WINDOW-01`, `LAYOUT-04`, `APPEAR-01`. Does not force onboarding. Engine bootstrap remains `SHELL-06` / `REL-C03`. |
| `SET-FIRST-USE` | Settings 初始化设置 dialog | Settings → `Menu.initSettings` → `openFirstUseSettings(false)` | Load-defaults button fills the dialog only. Confirm writes listed ui/leelaz keys and `config.save()`. | Confirm parse errors → hint, no write. Save IOException printed, not modal. | Source: `FirstUseSettings.java:33-755`; `Menu.java:5314-5323`. `openFirstUseSettings(false)` is the only caller; `FirstUseSettings(true)` close-exits-process has no caller. | Not required | Abandoned / redesign (09) | Do not migrate the forced wizard. `PREF-01` categorized Preferences plus contextual entry points. |
| `SET-PERSIST-CONFIG` | Persist ui + leelaz + logging settings to `config.txt` | Any ui/leelaz write; Settings/FirstUse Confirm; Appearance `persistUiSettings`; Help clear-personal-data; first-run `finalizeAutomaticFirstRunSetup`; shutdown `config.save()` | `createDefaultConfig` plus `opt*` plus first-launch. Atomic temp write. | Missing → write defaults + `newProfile`. Unreadable → backup + rebuild. Shutdown save fail → modal with path. | Source: `Config.initializeFromWorkDirectory` 1529–1571; `save` 3532–5339; `writeConfig` 2991–3015. View `toggleCoordinates` writes uiConfig only, no `save()`. | Not required | Next redesign (09) | `PREF-01` native durable-preference contract. Do not import Java `config.txt`. |
| `SET-PERSIST-WINDOW` | Persist window bounds, splitter shares, independent frames, GTP console, toolbar height | Implicit on close via `config.persist()`. Reset via `SET-RESET-WINDOW-POS`. Load: `fitWindowBounds` + `availableScreenWorkAreas` (`LizzieFrame.java:1121-1128, 1999-2041`); winrate-graph `1133-1136`; board proportion persist typo `1139` vs uiConfig `Config.java:1801` `board-postion-proportion`; shares `1140-1150`. GTP/independent frames load in their constructors. | Persist default: empty positions, `window-maximized=false`. Field `startMaximized=true` is **not** the persist default. | Load fail → in-memory empty persist. After window reset, writes skipped until restart. Off-display bounds fitted to current work areas. | Source: `Config.persist` 3017–3408; `createPersistConfig` 2957–2988; `LizzieFrame.java:1121-1150,1999-2041`. | Not required | Next redesign (09) | Split: `WINDOW-01` window geometry; `LAYOUT-02` workspace proportions. Neither owns semantic settings from other domains. |
| `SET-LANG` | Choose application locale | Settings language submenu; FirstUse language combo | New profile: OS locale. Load fallback SYSTEM (`use-language=0`). Persist `ui.use-language`. Settings `selectLanguage` writes uiConfig and `config.save()` immediately; FirstUse Confirm also saves. | Save IOException → `printStackTrace`; restart message is still shown. No live `ResourceBundle` replacement: `selectLanguage` loads the selected bundle only to obtain `Lizzie.hint.restart` (`Menu.java:6561-6574`). | Source: `AppLocale.java:7-14`; `Menu.java:5360-5393,6561-6574`. | Not required | Deferred (09) | `I18N-01` Complete Localization (Deferred). Partial translation is not equivalent language support. Roadmap assigns phase. |
| `SET-LOOKS` | Java L&F vs system L&F | Settings 界面外观; FirstUse radios | Load `optBoolean(..., !OS.isWindows())`. Persist `ui.use-java-looks`. Restart required. | SC-01. Settings-menu writes rely on later save. | Source: `Menu.java:5444-5471`; `Config.java:1619`. Settings-menu writes `uiConfig` without `save()`; crash-without-shutdown has no flush. | Not required | Swing-only implementation (09) | Exclusion — Swing/implementation-only. Next uses system-integrated chrome. |
| `SET-FRAME-FONT` | UI font size small/middle/big/other | Settings 界面字体大小; other → `SetFrameFontSize` | Load fallback 12. Small=12, middle=16, big=20; other **12–20** (`SetFrameFontSize.java:129`). Persist `ui.frame-font-size`. Apply always shows `menu.setFrameSizeRestart` (`LizzieFrame.setFrameFontSize` 19030–19047); no live UI-font reload. | SC-01 | Source: `Menu.java:5395-5442`; `Config.java:1837-1839`; `SetFrameFontSize.java:129`; `LizzieFrame.java:19030-19047`. | Not required | Abandoned (09) | Exclusion — Abandoned product capability. Follow system DPI / accessibility scaling. |
| `SET-SOUND` | Toggle move sound and mute-during-sync | Settings `playSound`, `notPlaySoundInSync` | Both load-fallback true. `play-sound` absent from default JSON. Persist `ui.play-sound`, `ui.not-play-sound-insync`. Listeners do not `save()`. | SC-01 | Source: `Menu.java:5475-5496`; `Config.java:102-103,1764-1765`. `Utils.playVoiceFile` when `playSound` at Board place/pass/navigation (`Board.java:2537,2580,2797,2993,4193`). `notPlaySoundInSync` returns before play when `urlSgf` or `syncBoard` (`Utils.java:1117`). Parsers and bulk rewrites temporarily force `playSound=false`. | Not required | Route to 03/06 (09); IDs in 10/13 | Split: `REVIEW-08` default-on persisted sound for successful local moves, passes, and forward review navigation. Mute-during-sync → `PROV-03` / `READ-02` (and other sync owners). `PREF-01` is persistence mechanism only. |
| `SET-CONTRIBUTE-MENU-VIS` | Show or hide the Contribute menubar | Settings `showContribute`; menu `setVisible` | Load `show-Contribute` default true. Persist `ui.show-Contribute`. | SC-01 | Source: `Menu.java:5498-5507,5576-5580`; `Config.java:2070`. | Not required | Route to domains 05/06 (09, 12, 24) | `CONTRIB-01` exposes complete service setup/run actions only after it is Accepted; `GAME-09` later adds Open Watch / Close Watch. Do not migrate a general visibility preference or show disabled “尚未接入” actions. |
| `SET-THEME-BOARD-STYLE` | Japanese vs Chinese-classic board style | View → 外观主题 → 棋盘风格 | JSON / load default Japanese. Persist `ui.board-style`. | SC-01 | Source: `Config.java:34-35,1780-1781,2674-2729`; `Menu.java:531-558`. | Not required | Next redesign (09) | `APPEAR-01` curated Classic (default) and High Contrast. |
| `SET-THEME-APPLE-CLASSIC-CUSTOM` | Apple-style UI, Morandi colors, custom board image | View Apple / classic color / set+clear custom board (`Menu.java:673-729`). Custom apply/clear call `persistUiSettings()`. | JSON `is-apple-style=false`, `theme=default`. Apple forces `use-morandi-colors`. Persist those plus custom image path. | SC-01 | Source: `Menu.java:673-729,6577-6608`; `Config.java:79,110,1580,1775,2890-2892`. | Not required | Abandoned (09) | Exclusion — Abandoned product capability. |
| `SET-THEME-DIALOG` | Full theme editor tab | Settings → 主题 → `openConfigDialog2(1)` | Theme `default`. Shadows JSON 85 vs field 75. Persist theme object + ui keys. | SC-01 | Source: `Menu.java:5347-5356`; `Config.readThemeVaule` 2193–2291. | Apply vs live preview matrix (dynamic visibility) | Abandoned (09) | Exclusion — Abandoned product capability. Curated `APPEAR-01` lives in `PREF-01`. |
| `SET-CONFIG-DIALOG-DISPLAY` | Multi-tab comprehensive settings | Settings 综合设置; `Shift+X` `Input.java:583-585` → `openConfigDialog2(0)`; Help About opens tab 2 | N/A as a dialog. Display checkboxes live-toggle View flags; other tabs OK-apply. | SC-01 | Source: `Menu.java:5335-5356`; `Input.java:583-585`; `ConfigDialog2.java:561-651`. | Full OK-apply vs live-toggle matrix beyond Display (persistence) | Next redesign (09) | `PREF-01` one categorized Preferences surface plus contextual actions. Each setting’s behavior stays with its owner. |
| `SET-NETWORK-PROXY` | HTTP proxy used by 06/07 traffic | ConfigDialog2 Advanced: Direct / System / Manual (`ConfigDialog2.java:3103-3234`). Host/port **enabled only in Manual**. | Default mode `direct`; host `127.0.0.1` port `7897`. Persist mode/host/port. | SC-01. Manual empty host refused. | Source: `NetworkProxy.java:19-27`; `Config.createDefaultConfig` 2883–2885; `ConfigDialog2.java:3227-3253`. | Not required | Split Next redesign / Abandoned (14, 22, 26) | Update traffic → `REL-03` OS/system proxy. Remote-provider traffic → shared Provider Network Policy on `PROV-01`–`PROV-07`, with platform resolution, environment override, `NO_PROXY`, system-managed proxy authentication/trust, actionable failure, and no direct fallback. The Java `direct`/`system`/`manual` UI and keys are Abandoned and not migrated; no independent or Deferred proxy item. |
| `SET-BOARD-SIZE` | Default board size (Display radios 19/13/9/7/5/4/other; SetBoardSize 19/15/13/9/other) | ConfigDialog2 Display radios **19/13/9/7/5/4/other**; `SetBoardSize` **19/15/13/9/other** via Edit / toolbar / `Ctrl+I`; `saveOtherBoardSize` | Default 19. Other size 21×21. Persist `board-size`, `other-size-width/height`. Commented `<2` clamp is **not live**. | SC-01 | Source: `ConfigDialog2.java:438-488`; `SetBoardSize.java:123-156`; `Config.java:2887,2030-2031,3789-3794`; `Menu.java:3945-3946,6110-6114`; `Input.java:501`. | Not required | Route to 03/05 (09, 10, 12) | Split: `SGF-10` New Document parameter; `GAME-04` match start. Does not destructively resize a populated tree. |
| `SET-LAYOUT-MODE` | ExtraMode / classic / custom layouts | View 布局模式 Alt+1..9 | JSON ExtraMode Normal. Unknown → Normal. Persist `extra-mode`, `is-classic-mode`, `custom-layout-1/2`. | SC-01 | Source: `ExtraMode.java`; `Menu.java:1778-2014`; `Config.java:1579-1581`. | Per-mode panel matrix (dynamic visibility) | Abandoned (09) | Exclusion — Abandoned product capability. Replaced by one adaptive workspace (`LAYOUT-01`/`LAYOUT-02`/`LAYOUT-04`). |
| `SET-LAYOUT-PANELS` | Show/hide panels and independent frames | View 面板 including subboard/winrate/comment/variation/list/captured/status/GTP, independent main/sub, hawk-eye (`panel.add(hawkEye)` `1338-1347`), SuggestionList; ConfigDialog2 Display live toggles | Default JSON: status/comment/variation/subboard/captured/winrate on. Persist show-* keys; independent/GTP geometry in persist. | SC-01 + SC-02 | Source: `Menu.java:1099-1369`; `Config.java:2844-2867,1650`. | Not required | Next redesign (09) | `LAYOUT-04` Rail Visibility (left/right collapse only). Do not migrate independent frames or arbitrary placement. Accepted `UI-01` is not expanded. |
| `SET-LAYOUT-TOOLBAR` | Toolbar visibility, wrap, order | View 工具栏: top separated/combined/hidden, auto-wrap (`Menu.java:4352-4455`); `ToolbarPositionConfig` 显示顺序设置 (`4557`) | `showTopToolBar=true`, `autoWrapToolBar=true`. Persist flags; height in persist. | SC-01 + SC-02 | Source: `Menu.java:487,4352-4557`; `Config.java:1103-1104,1904-1905,3070-3076`. | Not required | Abandoned (09) | Exclusion — Abandoned product capability. Next toolbar stays fixed. `UI-05` claimed rows remain unchanged. |
| `SET-BOARD-POS` | Nudge main-board splitter left/right | View 主棋盘位置 `[` `]`; `Input` `VK_OPEN_BRACKET` / `VK_CLOSE_BRACKET` (`751-757`) | Field default 4. Nudge ±1, clamp 0–8 (`nudgeBoardPositionProportion`). Persist typo `board-postion-propotion`; uiConfig `board-postion-proportion`. | SC-01 + SC-02 | Source: `Menu.java:490-516`; `Input.java:751-757`; `LizzieFrame.java:6788-6801`; `Config.persist` 3087; load 1801 and `LizzieFrame.java:1139`. | Not required | Next redesign (09) | `LAYOUT-01` / `LAYOUT-02` / `LAYOUT-03` (draggable proportions and narrow reset). |
| `SET-COORDS` | Show board coordinates | View 坐标(C); ConfigDialog2 Display | JSON / load default true. Persist `ui.show-coordinates`. `toggleCoordinates` writes uiConfig only (no `save()`). Next session-only is not this baseline. | SC-01 | Source: `Menu.java:518-529`; `Config.java:2669-2672,1798,2863`. | Not required | Route to domain 03 (09, 10) | `REVIEW-07` persists the Next boolean (default on) through `PREF-01`. |
| `SET-MOVE-NUMBERS` | Choose how move numbers render | View 手数(M) modes including custom `MovenumberDialog`; ConfigDialog2 radios | JSON `show-move-number=false`, `only-last-move-number=1`. Persist those plus branch/from-one flags. `MovenumberDialog`: empty or over-length text → 0; `<=0` refused with tooltip and red field; no other upper bound (`MovenumberDialog.java:106-134`). | SC-01 | Source: `Menu.java:561-899`; `Config.java:39-44,1573-1578,2843,2891-2893`; `MovenumberDialog.java:106-134`. | Not required | Route to domain 03 (09, 10) | `REVIEW-07` persists the Next all-move-number boolean (default off). Java last-N / custom / from-one / branch-numbering **Abandoned**. |
| `SET-SUGGESTION-INFO` | Candidate overlay WR/visits/score, order, delay, max-red | View 选点信息; FirstUse WR/visits/score | JSON `show-best-moves=true`, `limit-max-suggestion=10`. Persist ui + leelaz keys. Search/limit visits are 04. | SC-01 | Source: `Config.java:2826,2861,1736-1738,1862-1866`; `FirstUseSettings.java:471-519`. | Not required | Route to domain 04 (09, 11) | `ANA-04` supported fields, defaults, and persistence. Ticket 11 abandons Java delay, black/white filtering, PV-visits, branch-length, and other granular review display preferences. Do not expand `UI-03`. |
| `SET-NEXT-MOVE` | Next-move marker none/simple/info plus min playouts | View 下一手 including `minPlayoutsForNextMove` | JSON `show-next-moves=true`. Load blunder true; min playouts 30. Two booleans + min-playouts, not a Java enum. Persist those ui keys. | SC-01 | Source: `Menu.java:574-658`; `Config.java:1729,1620-1622,2865`. | Not required | Next redesign (17) | `ANA-10` owns the independent Next-move Review Marker: persisted `Off` / `Variations` / `Graded`, first-use `Variations`, visible and `J` entry points, shared Next grading, and no successor minimum-playouts preference. |
| `SET-WINRATE-GRAPH` | Graph perspective, WR/score lines, blunder bar, mouse-over WR | View 胜率图设置 | Blunder bar JSON false plus one-time hide migration. Lines default true. Persist ui flags; persist `winrate-graph` written `Config.persist` 3383–3385 and loaded `LizzieFrame.java:1133-1136` (missing or length ≠ 1 → skip restore). | SC-01 + SC-02 | Source: `Menu.java:1373-1394`; `Config.java:2850,2144-2158,1834-1836,3383-3385`; `LizzieFrame.java:1133-1136`. | Not required | Next redesign (18) | `ANA-11` owns Graph Perspective, Winrate Line / Score Lead Line, Blunder Bar, Graph Hover, Score Lead Scale, and `PREF-01` persistence. Chart presence stays `UI-01`. Click-to-jump stays `REVIEW-01`. Dual complementary curves are not this item. Do **not** expand `ANA-04`, `UI-01`, or `PREF-01`. |
| `SET-SUBBOARD` | Sub-board content: variation / raw / heatmap | View 小棋盘设置; FirstUse mouse-over-subboard radios | JSON `show-subboard=true`. Heat flags load false. Persist those ui keys. Heatmap **data** is 04; mode is 02 census. | SC-01 | Source: `Menu.java:1618-1771`; `Config.java:1597-1598,2097,2866`. | Four-sub vs single heatmap target (dynamic visibility) | Next redesign / Abandoned (19) | `ANA-12` owns the persisted Variation / Raw Sub-Board Content Mode. `ANA-04` still draws PV when mode is Variation. Heatmap-on-sub and mouse-over freeze are Abandoned. Mini-board presence stays `UI-01`. Do **not** expand `ANA-04`, `UI-01`, `UI-03`, or `PREF-01`. |
| `SET-MAIN-PANEL` | Large subboard/WR, append WR to comment, names on board, comment control, always-on-top, related hints | View 主界面设置 cluster; large-sub / large-WR toggles | JSON large flags false. `append-winrate-to-comment=true`. Persist ui keys. Hint flags also `SET-HINT-*`. | SC-01 | Source: `Menu.java:901-1007`; `Input.java` Ctrl+F / Ctrl+W / Ctrl+Z; `Config.java:2535-2576,1794-1797,2894`. | Not required | Split (09, 10, 20) | Split: `REVIEW-09` HUD player names; `WINDOW-02` main-window always-on-top. Abandoned: mutually exclusive large-sub / large-WR presets; writing generated winrate/score/playouts into personal `C` ([ADR 0003](adr/0003-do-not-append-generated-stats-to-personal-comment.md)). Comment-control hint remains `SET-HINT-COMMENT-CTRL`; new-board/replace hints remain `SGF-07`. Do **not** expand `UI-01`, `SGF-05`, `LAYOUT-01`, `WINDOW-01`, `REVIEW-07`, or `SGF-13`. |
| `SET-KATA-DISPLAY` | Score+komi, estimate placement/styles, ownership, shortcut | View KataGo settings; FirstUse score+komi radios | Estimate off; on-main/on-sub true. Persist ui keys. Computation is 04; overlay prefs were 02 census. | SC-01 | Source: `Menu.java:1773-1776`; `Config.java:1595-1617,1724-1725`. Commented score-perspective items are **not** Entry Points. | Not required | Route to domain 04 (09, 11) | Split: `ANA-04` keeps capability-gated ownership/policy (and candidate-limit) presentation. Remaining Java estimate-style / placement granularity is abandoned with Ticket 11’s granular review-display remainder. Do not expand `UI-03`. |
| `SET-HINT-NEWBOARD` | “No notice again” for new-board warning | 主界面设置 checkbox (`Menu.java:973-982`); new-board dialog `LizzieFrame.java:20376-20392`. **Not** reset by `resetAllHints`. | Load default true. Persist `ui.show-new-board-hint`. **Not** reset by `resetAllHints`. | SC-01 | Source: `Config.java:1173,1987`; `Menu.java:973-982`; `LizzieFrame.java:20376-20392`; `Lizzie.resetAllHints` (key absent). | Not required | Next redesign, domain 03 (09, 10) | Replace with the non-dismissible dirty-state gate of `SGF-07` / `SGF-10`. Not `GUIDE-01`. |
| `SET-HINT-REPLACE` | Confirm-replace-file hint | 主界面设置 checkbox (`Menu.java:984-993`); File replace dialog `LizzieFrame.java:4116-4141`; re-enabled by `SET-RESET-HINTS` | Default true. Persist `ui.show-replace-file-hint`. Reset **yes**. | SC-01 | Source: `Config.java:1172,1986`; `Menu.java:984-993`; `LizzieFrame.java:4116-4141`; `Lizzie.resetAllHints` 1520, 1524. | Not required | Next redesign, domain 03 (09, 10) | Same non-dismissible dirty-state replacement gate (`SGF-07`). Not education. |
| `SET-HINT-COMMENT-CTRL` | Hint when closing comment controls | Reset via `SET-RESET-HINTS` only. Load/reset/write exist; no constructed menu consumer. | Default true. Persist `ui.allow-close-comment-control-hint`. Reset **yes**. | SC-01. Flag is never read outside load/reset/write. | Source: `Config.java:1137,1657`; `Lizzie.resetAllHints` 1519, 1525. | Not required | Abandoned (09) | Exclusion — Abandoned product capability. |
| `SET-HINT-AUTOANALYZE` | Tip when leaving auto-analyze | Auto-analyze exit `LizzieFrame.java:12523-12544` (`JOptionPane` dialog when `exitAutoAnalyzeByPause` and `exitAutoAnalyzeTip`); reset via `SET-RESET-HINTS` | Default true. Persist `ui.exit-auto-analyze-tip`. Related `exit-auto-analyze-by-pause` is a separate pref, not in `resetAllHints`. | SC-01 | Source: `Config.java:1282-1283,2044-2045`; `LizzieFrame.java:12523-12544`; `Lizzie.resetAllHints` 1522, 1526. | Not required | Abandoned (11, 21) | Exclusion — Abandoned with `CAP-04-ANA-08`. The pause-exit educational object does not exist in Next. Do not bind `GUIDE-01` to this path. |
| `SET-CLEAR-PERSONAL-HISTORY` | Remove stored personal histories without deleting persist or engines | Help → `clearAllPersonalData` with confirm | N/A. Removes `fox-recent-searches`, `recent-files`, `batch-analysis-history`, `share-history` then `config.save()`. Does **not** remove `recent-file-paths`. | Cancel → no-op. On OK, four keys are removed from in-memory `uiConfig` first, then `config.save()`. Save IOException is printed; the success dialog is still shown. Durable file stays unchanged until a later successful save. | Source: `Menu.java:5225-5253`. | Not required | Route to 03/06 (09, 10, 13, 14) | Split by data owner: `SGF-09` recent kifu; `PROV-02`/`PROV-04` (and Yike recents under `PROV-01`/`PROV-03`) provider/search history; batch-analysis history follows deferred `ANA-07`. No broad preference reset. Not a domain-07 item (`REL-C13`). |

---

## Domain 03 — SGF, board, and no-engine review

Independent census: [research 03](../.scratch/migration-baseline-v1-capability-audit/research/03-sgf-board-review.md) against Java `7b4027531c2b26062d0bfc27a040cc550cfbea4d`. Prior coverage-audit census: [research 03](../.scratch/migration-coverage-audit/research/03-sgf-board-review.md). Dispositions: [Ticket 10](../.scratch/migration-coverage-audit/issues/10-decide-sgf-board-review-dispositions.md), with remainder closures in Tickets 17, 20, 27, and 28. Accepted `SGF-01`–`SGF-06`, `RULE-01`, `UI-01`, `UI-03`, `UI-04`, claimed `UI-05`, and Partial `UI-02` remain complete at original scope. Adjacent extras are successor / redesign / defer / abandon rows, not silent expansions.

**Computed Domain 03 Capabilities: 35.** Reference 35. Extra/missing rows: none. The count was computed from registered, user-reachable surfaces; 35 was compared afterward and was not a stop condition.

Domain 03 owns File/SGF/board/review Entry Points, personal-comment mutation, and no-engine review tools. Runtime-check is this domain's dedicated column. Provider fetch and engine-produced analysis are links only.

### Frozen Accepted R1/R2 claims
| Frozen ID | Name | User-reachable Entry Points | Default / persistence | Failure / recovery | Frozen evidence | Runtime-check | Disposition | Mapping |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `SGF-03-RT` | Parse → edit → serialize → reparse preserves tree meaning | File Open / Save / Save As / clipboard copy serialize / Next parse-textarea / sample load | FF[4]-family SGF; board size from `SZ` (parser default 19); `loadSgfLast` default **false** (`load-sgf-last`) | Unreadable/empty: `SgfObservation` + `false`; Next malformed: `MalformedSgf`, holder unchanged | Source: `SGFParser.java` load 51–112, `loadFromString` 114–142; `SGFParserSemanticRoundTripTest.java` 28–80; `SGFParserEmptyEngineSaveTest.java` 14–51 | Not required | Frozen Accepted boundary (10) | `SGF-01`. Adjacent GIB/raw-save/unknown-property extras are not this item. |
| `SGF-03-DTO` | Complete tree + `NodePath` as location | N/A (wire contract consumed by current-game UI) | Root `[]`; default selected path first-child mainline to leaf | Invalid path → `InvalidNodePath`; no document → `NoCurrentGame` | Source: `BoardHistoryList` / `BoardHistoryNode`; sibling structure in `SGFParserSemanticRoundTripTest` 43–62 | Not required | Frozen Accepted boundary (10) | `SGF-02` |
| `SGF-03-NAV` | Parent / child / sibling / first / last / page jumps | Java arrows / Page / Home / End; Edit jump items; toolbar first/last/±1/±10/gotomove; wheel; right-click previous | Session cursor. Java `loadSgfLast` persists whether to land on last move | Illegal path rejected; Next select does not bump generation | Source: `Input.java:268-282,326-410,560-581`; `Menu.java:3842-3880`; `BottomToolbar.java:1266-1384`; `LizzieFrame.java:18882-19019` | Not required | Frozen Accepted boundary (10) | `SGF-03`. Click-on-tree / graph / try-play jumps are adjacent, not this item. |
| `SGF-03-EDIT` | Play a legal point or review pass as a child; remove selected non-root variation | Game 停一手(P) `board.pass()`; `Input` VK_P (HumanSL → 05); board left-click; Shift+Delete / Edit 删除分支; top-strip `playPass` when double-menu shown | Color = selected node’s player-to-play. Dirty until Save | Occupied/suicide/simple-ko reject without mutation. Root cannot be removed | Source: `Menu.java:3141-3149`; `Input.java:431-437,678-689`; GIB `SKI` → pass | Not required | Frozen Accepted boundary (10) | `SGF-04` (pass insertion in frozen fixtures); claimed `UI-05` `P` and Shift+Delete. Delete-one-move / setup / drag / set-as-main are successors. Match pass is 05. |
| `SGF-03-CMT` | Edit/clear personal comment; generated info does not overwrite personal field | Java comment pane `setCommentEditable`; Next AnalysisPanel personal textarea | Empty comment. Persist `C` in serialized SGF | Next no-op if unchanged; empty trim removes `C` | Source: `TeacherCommentCodec.java:7-75`; `CommentDisplayRenderer.java:11-49`; `LizzieFrame.setCommentEditable` 9890–9916 | Not required | Frozen Accepted boundary (10) | `SGF-05`. Teacher/match-info/engine-game layers are adjacent (`SGF-03-ADJ-COMMENT-LAYERS`). |
| `SGF-03-SAVE` | Serialize current tree to current path or chosen path | File Save / Save As; Ctrl+S / S; toolbar `savefile`; top-strip `btnSave`. Extra save shortcuts belong to ADJ-SAVE-MORE | Timestamp-ish filename from GameInfo or `yyyyMMddHHmmss`; last-folder in `filesystem` | Java: `saveFileFailed` toast. Next: directory-as-file leaves path/dirty; cancel/denied write does not adopt path. `.gib` cannot overwrite as GIB | Source: `LizzieFrame.java:4042-4250,4256-4330`; `Menu.java:225-262` | Not required | Frozen Accepted boundary (10) | `SGF-06`. Raw/branch/image outputs are adjacent. |
| `SGF-03-RULE` | Occupied, capture, suicide, pass, simple ko; rejected edit leaves tree unchanged | Same as `SGF-03-EDIT` | Simple ko; suicide forbidden in Next `go-core` | Atomic reject | Source: `GIBParser.place`/`pass` 94–105; `src/test/java/featurecat/lizzie/rules/*` | Not required | Frozen Accepted boundary (10) | `RULE-01` for frozen R1 fixtures. Japanese/Chinese/Tromp-Taylor engine-rule UI is 04/02, not this item. |
| `SGF-03-CHROME` | Workbench: board, analysis/comment rail, win-rate, candidates, mini-board | Main window after open | Next fixed 228px/260px rails (`UI-01`). Layout visibility is 02 | N/A | Source: `LizzieFrame` layout (`showComment` / `showVariationGraph` / `showSubBoard`) | Not required | Frozen Accepted boundary (10) | `UI-01`. Resizable splitters → `LAYOUT-01`. |
| `SGF-03-BOARD-INTENT` | Pointer/keyboard play intent updates selected node without blocking render | Board click; board-focused arrows+Enter/Space; outside-board arrows navigate | N/A | Occupied: Next status text, position unchanged | Source: `Input.java:19-80` | Not required | Frozen Accepted / Partial residual (10) | `UI-02`. Remaining Partial gap is engine-event delivery while a mutation promise is pending (**04**), not a 03 expansion. |
| `SGF-03-HOVER` | Hover previews a candidate without committing; scope change drops stale hover | Pointer over candidate; leave/click/scope change cancels | Java default **200 ms** plus optional delay dialog. Next accepted contract is **120 ms**. Delay settings in `uiConfig` | Scope mismatch refuses publish | Source: `SuggestionHoverIntent.java:1-65`; `AnalysisCandidateValidator.java:10-65`; `LizzieFrame.java:8810-8849,12844` | Not required | Frozen Accepted boundary (10) | `UI-03` at the frozen Next 120 ms contract. Java 200 ms + delay dialog is `SGF-03-ADJ-HOVER-DELAY`, not this item. |
| `SGF-03-NOENGINE` | Open, inspect, edit, save, exit without engine profile/process | All File/SGF/board/review controls; Next chip `未加载引擎` | Next `engineLabel` default `未加载引擎` | Engine-only actions unavailable/explanatory | Source: `SGFParserEmptyEngineSaveTest.java:14-37` | Not required | Frozen Accepted boundary (10) | `UI-04` |
| `SGF-03-ACTIONS` | One visible control + intended shortcut for the **claimed** R2 set | `App.tsx` onKey; `AppChrome` File/View/Game/Edit/toolbar. Java `Input.keyPressed` is a larger map | Coordinate/move-number toggles are session in current Next (persist via `REVIEW-07`) | `shouldIgnoreApplicationShortcut` skips when typing in fields | Source: `Input.java:312-823`; Ticket 07 claimed inventory | Not required | Frozen Accepted boundary (10) | `UI-05` for claimed rows only. Disabled `尚未接入` rows stay unclaimed here and are inventoried as adjacent Capabilities below. |

### Adjacent successor surfaces

| Frozen ID | Name | User-reachable Entry Points | Default / persistence | Failure / recovery | Frozen evidence | Runtime-check | Disposition | Mapping |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `SGF-03-ADJ-OPEN` | Choose a kifu file and install it as the current game | File 打开, `O`, toolbar `openfile`, top-strip `btnOpen`; `loadFile` after chooser | last-folder from persisted filesystem | IO / parse false → restore sound flag + later open-failed message. Java has **no** dirty-discard confirm on Open (unlike paste). Refuses load during engine/human match. `SGFParser.load` clears the board **before** exists/empty/parse; a failed SGF open leaves a cleared board. `openFile` assigns `curFile` when the chooser returns a file even if load failed | Source: `Menu.java:196-205,8174-8175`; `Input.java:532-541`; `BottomToolbar.java:1347-1352`; `LizzieFrame.java:4332-4365,4475-4521,4974-5030`; `SGFParser.java:51-112` | Not required | Next redesign (10) | `SGF-07` Safe Current-game Replacement. Native activation / drop invoke this seam through `APP-01`/`APP-02`. |
| `SGF-03-ADJ-GIB` | Load `.gib` (Tygem) into the current board | Same Open chooser (`*.gib`); `loadFile` extension branch | Names “Player 1/2”; komi 7.5 if not parsed (`readKomi` gates GIB komi). Save as SGF | Missing/unreadable/empty → false **before** clear. Non-empty file clears then parse. Handicap >9 clamped to 9; 5/7 place tengen `(9,9)` extra then remaining `handicapPlacement` stones | Source: `GIBParser.java:1-124`; `LizzieFrame.loadFile` 4994–4996; `GIBParserTest.java:1-80` | Not required | Equivalent migration (10) | `SGF-08` through `SGF-07`. GIB is import-only; never overwrite as GIB. |
| `SGF-03-ADJ-RECENT` | Re-open a recently used kifu | File → 最近打开 (`updateRecentFileMenu`) | Cap **5**. Persist `uiConfig` `recent-file-paths`. Newest last; menu lists newest first | `loadFile(recent, true, false)` does not rewrite recents/last-folder. Missing path still runs `SGFParser.load` (clears then false) and the later open-failed message. Help clear-personal-data removes `recent-files`, **not** `recent-file-paths` | Source: `Config.java:2161-2191`; `Menu.java:169,207-210,5225-5253,6721-6744`; `LizzieFrame.loadFile` 5005–5008 | Not required | Equivalent migration (10) | `SGF-09` via `SGF-07`; persist through `PREF-01`; owns clearing this history. |
| `SGF-03-ADJ-CLIP` | Copy current tree as SGF text; paste SGF text as current game | File copy/paste; Ctrl+C / Ctrl+V | N/A | Copy exception logged; paste ignore or confirm-cancel leaves game. Java `isSGF` gate + confirm-if-stones | Source: `LizzieFrame.java:9736-9801`; `Menu.java:371-396`; `Input.java:544-550,643-650` | Not required | Split accepted / Next redesign (10) | Clipboard **copy** remains claimed `UI-05`. Clipboard **paste** belongs to `SGF-07` (reject empty/invalid before replacement). |
| `SGF-03-ADJ-SAVE-MORE` | Save raw SGF, raw+comments, current branch flattened, board/sub-board/winrate images | File → 更多保存; Ctrl+Shift+S, Ctrl+Alt+S, Alt+S, Shift+S, Shift+Alt+S; File copy-board / copy-subboard Shift+C / Alt+C | Image chooser starts at shared `last-image-folder` (fallback `last-folder`) with no prefilled name. Java writes `last-image-folder` on chooser approval before overwrite/write outcome | Existing-file confirm; unsupported format dialog; IO failures may be swallowed after the directory changed | Source: `Menu.java:247-325`; `Input.java:506-529`; `LizzieFrame.java:4042-4112,4256-4330,10652-10918` | Not required | Abandon / Deferred (10, 27) | Split: abandon exact Swing raw/raw-with-comment modes and Sub-Board Image Export. Defer `EXPORT-01` selected-branch SGF export, `EXPORT-02` Review-board Image Export, and `EXPORT-03` Winrate Chart Image Export. The domain-04 image surfaces in this frozen row are the Sub-Board and winrate chart. |
| `SGF-03-ADJ-TEMP` | Named slot save/load with thumbnail; optional resume; autosave on exit | File → 存档与读档 → `showTempGamePanel`; toolPanel `saveLoad` when `showSaveLoadMenu`; panel checkboxes. File menu Resume item is **commented out**; startup `resumeFile` remains | `auto-save-exit` default **true**; `resume-previous-game` default **false**. Persist those ui keys; slot files under `save/`. File menu Resume item is **commented out** | Missing bmp/sgf: stack trace / skip. Startup resume wiring is Domain 01 `SHELL-05` | Source: `Menu.java:327-339,412-425,9709,9732-9735`; `LizzieFrame.java:4523-4534,13608-13670,14000-14034`; `TempGameData.java`; `Config.java:1703-1704,2881` | Not required | Absorbed redesign / Abandoned (10) | `APP-04` owns automatic current-game session recovery. Abandon manual thumbnail slots, slot files, and a second autosave-on-exit model. |
| `SGF-03-ADJ-NEW` | Replace current game with an empty document | File 新建棋盘 / toolbar / top-strip `newEmptyBoard`; Edit 清空棋盘 Ctrl+Home `Board.clear(false)`. Java `N` is **05** genmove, not this Capability | Next empty `(;GM[1]FF[4]SZ[19]KM[7.5]PB[黑]PW[白])`. Java File-new / toolbar New call `newEmptyBoard` → in-place `board.clear(false)` and do **not** reset `curFile`. Edit 清空棋盘 is `Board.clear(false)` without the new-board dialog | `newEmptyBoard` confirm-cancel (when the current node has previous or next) leaves the game. Match play refuses File new | Source: `Menu.java:186-194,3809-3817`; `Input.java:565-569`; `LizzieFrame.java:20374-20420` | Not required | Next redesign (10) | `SGF-10` one New Document meaning for File New, `N`, Ctrl+Home, and Clear Board via `SGF-07`. |
| `SGF-03-ADJ-KOMI` | Toggle applying SGF/GIB komi into the current game | File checkbox 自动加载棋谱中的贴目 | Field and `optBoolean("read-komi", true)`. Absent from `createDefaultConfig`. Persist `uiConfig.read-komi`. Even when off, setup/handicap SGF still apply parsed root KM | N/A | Source: `Menu.java:398-410,459-464`; `Config.java:96,1741`; `SGFParser.java:150-174`; `GIBParser.java:107-109` | Not required | Next redesign / Abandoned (10) | Parsed root `KM` is authoritative for SGF and GIB (`SGF-07`/`SGF-08`). When absent, New Document default (`SGF-10`). Abandon the Java preference that conditionally ignores file komi. |
| `SGF-03-ADJ-SETUP` | Interactive starting-position setup | Game → 起始局面设置; board clicks in setup; top-strip `btnChangeTurn` | Tools black/white/erase. Persist setup stones in tree. AB/AW/AE/PL **parse** already in `SGF-01` | Non-root edits rejected with `setupEditRequiresRootOnly` (no mutation). Convert-current-position confirm-cancel returns false | Source: `Menu.java:3153-3254,8005-8014,8185`; `Input.java:46-62`; `LizzieFrame.java:8164-8240` | Not required | Successor migration (10) | `SGF-11`. Do not expand `SGF-01`/`SGF-04`. |
| `SGF-03-ADJ-INSERT` | Force black/white/alternating add; insert-into-list; drag a stone; double-click search; click-to-review | Edit menu; right-click add black/white / find / move / switch / review (`RightClickMenu` / `RightClickMenu2`); top-strip color buttons; `allowDrag` / `allowDoubleClick` / `enableClickReview` | Those booleans in uiConfig | Drag onto occupied: abort | Source: `Menu.java:3705-3781,8202-8207`; `RightClickMenu.java:25-27,133-138,267-287`; `RightClickMenu2.java:110-176`; `LizzieFrame.java:10300-10340` | Not required | Split redesign / Deferred (10) | Next-native click navigation → `REVIEW-01`. Defer forced colored insertion, list insertion, and stone drag as `SGF-15`. Defer board-point move search as `REVIEW-04`. |
| `SGF-03-ADJ-DELETE-MOVE` | Delete current move; undo/redo that edit | Edit 删除一手; Delete/Backspace; toolbar `deleteMove`; right-click `deleteone`; undo/redo delete | N/A. In-tree | Root delete **clears** the board (`clearBoardState`). Child-present confirm-cancel is a no-op | Source: `Menu.java:3884-3926`; `Input.java:294-299,678-689`; `BottomToolbar.java:1179-1183`; `RightClickMenu2.java:23-24,174-176`; `Board.java:4695-4799` | Not required | Successor migration (10) | `SGF-12`. Existing branch removal in `SGF-04`/`UI-05` unchanged. |
| `SGF-03-ADJ-MAIN` | Promote current variation to main; walk back to main trunk | Edit 设为主分支(L) / 返回主分支(B); `L` / `B`; toolbar `setMain`/`backMain`; top-strip `btnSetMain`/`btnBackMain` | N/A. Tree child order | Already-main is no-op | Source: `Menu.java:3820-3838,7981-8002,8186-8187`; `Input.java:423-428,663-676`; `BottomToolbar.java:1228-1238`; `LizzieFrame.java:18984-19019` | Not required | Successor migration (10) | `SGF-12` (same tree-order contract). Navigation *along* mainline remains `SGF-03`. |
| `SGF-03-ADJ-XFORM` | Transform whole tree geometry/colors | Edit exchange/spin/mirror; Ctrl+Shift+Alt+Right, Ctrl+Alt+arrows | N/A. Mutates tree | Non-square board refuses rotate; match play refuses. Otherwise clear + replay movelist; `curFile` restored | Source: `Menu.java:3953-3997`; `Input.java:330-402`; `Board.java:6099-6148` | Not required | Deferred (10) | `SGF-16` |
| `SGF-03-ADJ-META` | Edit PB/PW/KM; set SZ | Edit 编辑棋局信息(I) / 设置棋盘大小(Ctrl+I); toolPanel `setBoardSize` when `showGobanMenu` | Current GameInfo. Handicap field read-only. Persist root SGF properties on save | Whole-game analysis blocks edit | Source: `LizzieFrame.editGameInfo` 4012–4023; `GameInfoDialog.java:20-159`; `Menu.java:3930-3949,9708,9727-9730` | Not required | Split successor (10) | `SGF-13` editable player names and komi. Board size is `SGF-10` New Document parameter. Metadata *round-trip* stays `SGF-01`. |
| `SGF-03-ADJ-MARKUP` | Place/remove list-property marks on points | Top-strip markup tools when `isShowingMarkupTools`; `tryToMarkup` / `tryToRemoveMarkup`. Type 2 circle button is constructed **not** added | Markup off. Persist node properties; `SGF-01` already preserves unknown/list props in frozen set | Click off-board: false | Source: `LizzieFrame.java:14419-14478`; `Menu.java:8028-8198`; `SGFParser` `listProps` 41–43 | Not required | Successor migration (10) | `SGF-14`. Parse preservation stays `SGF-01`. Java circle tool is not an Entry Point; Next `SGF-14` still includes circles |
| `SGF-03-ADJ-AUTOPLAY` | Timed main-board / sub-board / variation replay | View 自动播放(Ctrl+A) → `AutoPlay` dialog; toolbar `autoPlay`. Next Ctrl+A toggles 800 ms | Toolbar main/sub values; persist branch-preview and engine-continuation defaults; JSON `replay-branch-interval-seconds` **0.9** for new profile | Next main-board autoplay stops at a leaf. Java candidate-variation replay stops when disabled | Source: `Menu.java:578-587`; `Input.java:710-713`; `BottomToolbar.java:1171-1176`; `AutoPlay.java:44-206`; `LizzieFrame.java:10171-10223` | Not required | Frozen accepted toggle / Deferred / Next redesign / Abandoned (10, 28) | Split: claimed `UI-05` fixed-interval Review Autoplay remains unchanged; configurable main-board timing stays Deferred `REVIEW-05`; candidate and Sub-Board Variation Replay map to Missing R4 `ANA-13`; abandon the full-variation-first pause and both leaf-only and every-beat Engine Continuation. `Ctrl+A` remains `UI-05`. Do not expand `ANA-04`, `ANA-12`, or `PREF-01`. |
| `SGF-03-ADJ-NEXT-HINT` | Show next-move marks: none / simple / with blunder info | View 下一手; `J` toggles | JSON `show-next-moves=true`. `show-next-move-blunder` load-fallback true. Persist those ui keys | N/A | Source: `Menu.java:574-598,2556-2568`; `Config.java:83,1620,1729,2646-2662,2865` | Not required | Next redesign (17) | `ANA-10` marks the selected SGF node's coordinate-bearing child moves, emphasizes its Primary Child, and grades that child only from a compatible current or cached Analysis Context. |
| `SGF-03-ADJ-TREE-CLICK` | Click variation graph, winrate graph, or blunder/suggestion table to change node | `VariationTree` clickPoint; blunder table mouseClicked | N/A | N/A | Source: `VariationTree.java`; `LizzieFrame.java:12008-12300,1431-1448` | Not required | Next redesign (10) | `REVIEW-01` Next-native tree/list exact `NodePath` selection. Domain-04 graphs call the same contract. |
| `SGF-03-ADJ-TRYPLAY / SCORE / LADDER` | Try-play overlay; score mode (area/territory + Confirm Result); auto-continue ladder | Try-play: `V` `tryPlay(false)`; toolbar `tryPlay(true)`. Score: Game 形势判断; Ctrl+Q; toolbar `finalScore`; board left-click in score; rule toggle and Confirm Result. Ladder: Game 继续征子 | Score defaults to area; selected rule persists `use-territory-in-score`. Ladder refuses below `MINIMUM_LADDER_LENGTH_FOR_AUTO_CONTINUATION` | Score preview is transient. Ladder failure must not partially mutate. Try-play must not mutate the authoritative tree | Source: `Input.java:64-67,554-557,704`; `Menu.java:3009-3011,3273-3287`; `BottomToolbar.java:1193-1197,1386-1391`; `Board.java:6540-6673`; `ScoreResult.java:178-387`; `SGFParser.java:1192-1211,1288-1311`; `Config.java:1237,2098`. One research heading; Ticket 10 split the three keys | Not required | Split successor / equivalent / deferred (10) | Resulting items in this one census row: `REVIEW-02` try-play sandbox; `REVIEW-03` dead-group marking plus area/territory scoring (equivalent; Confirm Result writes root `RE`); `REVIEW-06` ladder continuation (Deferred). Split keys `SGF-03-ADJ-TRYPLAY`, `SGF-03-ADJ-SCORE`, `SGF-03-ADJ-LADDER`. |
| `SGF-03-ADJ-URL` | Paste/open a URL and load SGF into the current game | File 打开在线链接(Q); `Q`; `OnlineDialog` | Refresh / live flags owned by 06 | Fetch/parse failure owned by 06; current-game install is 03 | Source: `Input.java:701-707`; `OnlineDialog.java`; `Menu.java:212-222` | Not required | Route to domain 06 (10, 13) | Domain 06 owns fetch (`PROV-01` / `PROV-03` / deferred `PROV-05`). Successful content enters through `SGF-07`. |
| `SGF-03-ADJ-PROVIDER` | Provider/readboard import as current-game replacement | Owned by 06; Java loaders end in `loadFile` / `loadSgfString*`; Next `handleProviderImport` | N/A as a 03 setting | Dirty confirm / owner rejection preserves current game | Source: `LizzieFrame.java:4650-4856`; research 03 § adjacent provider | Not required | Route to domain 06 (10, 13) | Same seam: 06 fetch, `SGF-07` install. Do not duplicate 06 Capabilities. |
| `SGF-03-ADJ-HOVER-DELAY` | Java 200 ms hover vs claimed 120 ms; delay dialog / manual-F reveal | `SetDelayShowCandidates`; `delay-show-candidates`, `delay-candidates-seconds` | Java `DEFAULT_DELAY_MS = 200` | N/A | Source: `SuggestionHoverIntent.java` default delay; research 03 hover adjacent | Not required | Abandoned (10) | Keep accepted `UI-03` at fixed Next 120 ms. Do not migrate Java 200 ms, delay setting, or manual reveal. |
| `SGF-03-ADJ-COMMENT-LAYERS` | Teacher markers, match-info divider, engine-game generated text in the comment pane | Comment pane display path; teacher codec; engine-game generated comments | `append-winrate-to-comment` default true (02/04 overlap) | N/A for the personal field | Source: `TeacherCommentCodec.java`; `CommentDisplayRenderer.java`; GAME-03 generated facts | Not required | Route to 04/05 (10, 11, 12, 20) | Generators: domain 04 analysis/teacher; domain 05 `GAME-05` session generated text. `SGF-05` owns only separation from personal `C`. Ticket 20 Abandoned writing generated winrate/score/playouts into personal `C`. Opening a Java mixed `C` treats the entire property as personal comment. Deferred `ANA-08` stays structured analysis-header exchange. |

---


## Domain 04 — Foreground engine and analysis

Independent census: [research 04](../.scratch/migration-baseline-v1-capability-audit/research/04-audit-foreground-engine-analysis.md) against Java `7b4027531c2b26062d0bfc27a040cc550cfbea4d`. Prior coverage-audit census: [research 04](../.scratch/migration-coverage-audit/research/04-engine-analysis.md). Dispositions: [Ticket 11](../.scratch/migration-coverage-audit/issues/11-decide-engine-analysis-dispositions.md), with adapter/game supersession from [Ticket 12](../.scratch/migration-coverage-audit/issues/12-decide-game-mode-dispositions.md) and remote remainder from [Ticket 23](../.scratch/migration-coverage-audit/issues/23-decide-ssh-remote-compute.md). Accepted `ENG-01`, `ANA-05`, `UI-03`, and `UI-04` keep original scope. Profile file format is persistence evidence, not a Capability. `EngineStartupBootstrap` is **absent** from the frozen Java tree.

**Computed Domain 04 Java Capabilities: 23.** Reference 25. Extra Java rows: none. Reference-only Frozen IDs that are not Java Capabilities (kept for Ticket 11 continuity): `CAP-04-ANA-05` (Next one-shot; Java has no named one-shot JSONL job) and `CAP-04-ANA-14` (Next SQLite; no Java analogue). The count was computed from registered, user-reachable Java surfaces; 25 was compared afterward and was not a stop condition.

Domain 04 owns foreground-engine catalog/lifecycle and analysis workflows only. Runtime-check is this domain's dedicated column. SGF comment mutation, persistence, and `EXPORT-01`/`EXPORT-02` remain Ticket 03; this domain records engine-produced comment data and links those items.

| Frozen ID | Name | User-reachable Entry Points | Default / persistence | Failure / recovery | Frozen evidence | Runtime-check | Disposition | Mapping |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `CAP-04-ENG-01` | Create/edit/delete/reorder saved engine profiles | `MoreEngines` table add/save/cancel/delete/move; command scan; remote SSH fields; GTP config; encrypt command | New row: empty command, localized “new engine”, `preload=false`, 19×19, komi 7.5. Persist `leelaz.engine-settings-list[]` + `ui.default-engine` | Save/scan/GTP load failures show dialogs. Empty command on dirty check offers delete. Deleting a running profile is **not** guarded in sampled handler (`updateEngines` then missing running name → `killAllEngines`). | Source: `EngineData.java:5-34`; `MoreEngines.java:69-234,516-567,777-806,1202-1331`; `Config.java:645-760` | Scan of a live directory (dynamic visibility); encrypt round-trip on a real password (persistence). | Frozen Accepted boundary / split successors / Deferred remote successors (11, 12, 23) | Split: keep `ENG-01` KataGo analysis fields/persistence/asset checks. `ENG-09` owns local adapter kind, shared executable/args/workdir, adapter-owned settings, and declared capabilities. `ENG-06` owns the Autoload Default; `ENG-08` defers manual catalog ordering. Board size/komi move to `GAME-04`. Abandon Java `initialCommand`, live GTP configuration UI, default-index, and per-profile preload. Deferred `SSH-01` owns user-managed SSH Engine Profiles for admitted stdio adapters. Deferred `RCOMP-01` owns separate Zhizi and Custom `ws/wss` provider configuration without synthetic catalog rows. Dedicated-analysis SSH is excluded; Contribute SSH remains Ticket 24. `SSH-01` and `RCOMP-01` do not expand `ENG-01` or `ENG-09`. |
| `CAP-04-ENG-02` | Four-state engine autoload at startup | `LoadEngine` radios; `MoreEngines` same radios; first-run bundled preference | Existing `config.txt`: manual (all flags false). New complete package profile: autoload default. Persist `autoload-default/last/empty` | Broken default + autoload-default may retarget bundled engine. Profile save failure → `EngineStartupStatus.failed`. No bundled engine and not autoload-empty → `needsRepair` | Source: `Lizzie.java:415-453,916-940,967-1045`; `LoadEngine.java:77-399`; `MoreEngines.java:777-806`; `Config.java:762-795` | LoadEngine first-launch picker vs autoload when the catalog is empty (also `SHELL-06`) (dynamic visibility) | Next redesign (11) | `ENG-06` zero-or-one Autoload Default. First use has no mark and starts with no engine. No startup chooser. |
| `CAP-04-ENG-03` | Persist last primary engine index for autoload-last | Implicit on shutdown when autoload-last is set; Settings last-engine checkbox; LoadEngine `rdoLastEngine` | Missing → `-1`. Persist `ui.last-engine` | Persist failure shows modal; does not block exit after the dialog. Corrupt index → empty/failed start, not a different profile. | Source: `Lizzie.java:426-428,1432-1460` | Whether shutdown persist runs after a crash kill (source is normal shutdown). | Abandoned (11) | Exclusion — Abandoned product capability. `selected_profile_id` remains Engine Settings editing identity, not a startup or run identity. |
| `CAP-04-ENG-04` | Use the desktop without a foreground engine | Autoload-empty; `LoadEngine` 不加载引擎; failed-start fallback `start(-1,false)`; `read` CLI (Abandoned as `SHELL-04`) | N/A. Autoload-empty flag only | Failed `EngineManager` construct → retry empty manager. Empty-by-choice is success, not failure. | Source: `Lizzie.java:429-430,440-441,996-1010`; JAVA_BASELINE `#348` | Overlay chip hidden when empty-by-choice vs start-failed (dynamic visibility) | Frozen Accepted boundary / lifecycle split (11) | Keep `UI-04` unchanged. `ENG-02` adds the authoritative no-engine run state. |
| `CAP-04-ENG-05` | Start the selected saved profile as the primary engine | Autoload paths; `LoadEngine` OK/double-click; menu engine items; `ChooseMoreEngine`; `EngineManager(config,index,loadDefault)` | Ponder starts unless `notStartPondering`. `playponder` field default true. Run does not persist across process restart. | Primary failures stay on repair status (`NEEDS_REPAIR` / `START_FAILED`). Stale generation cannot publish checking/ready. Exclusive lease → `existing_lease`. Remote compute may remap startup index (06). | Source: `Lizzie.java:77-118,967-1045,1048-1177,1210-1260`; `EngineStartupStatus.java:8-147`; `EngineFailedMessage.java:106-131`. Tests: `EngineStartupStatusTest`; `EngineManagerInitialStartupSynchronizationTest`; `EngineManagerLifecycleReservationTest.initializationCommandFailureCannotPublishReadyOrQueueReadyUi` | Overlay copy when snapshot `isActionable`; whether first-session suppresses the interactive diagnostic (`shouldOpenInteractiveDiagnostic`) (dynamic visibility) | Next redesign (11, 12, 23) | `ENG-02` owns the adapter-backed Foreground Engine Run. `KataGoAnalysis` is the default full-analysis adapter; `ENG-10` owns bounded Generic GTP game readiness. Exact position admission belongs to the adapter and domain-05 Match Reservation. Deferred `SSH-01` and `RCOMP-01` must enter the same run lifecycle; provider start is explicit, and remote start/auth failure never remaps or falls back to a local engine. |
| `CAP-04-ENG-06` | Stop or restart current / other / all engine runs | `Menu.shutdownCurrentEngine`, `restartCurrentEngine`, `shutdownOtherEngine`, `shutdownAllEngine`; secondary equivalents. Space / toolbar `analyse` pauses ponder (`CAP-04-ANA-04`), not process kill. `Menu.pondering` is declared and not added — not an Entry Point. | N/A. Stop current → `currentEngineNo=-1`, `isEmpty=true`. Restart restores ponder intent after 200 ms shutdown sleep. | Failed restart uses failed-switch/quarantine paths. Setup-mode reject. Lease in use → process not killed. Empty primary restart no-ops. `forceKillAllEngines` is `SHELL-08`, not this menu. | Source: `Menu.java:5734-5791`; `EngineManager.java:4025-4380`. Tests: `EngineManagerInitialStartupSynchronizationTest`; `Ticket07RestartBootstrapProductionEntryTest` | User-visible difference of `forceQuit` vs `normalQuit` (failure). Click Restart during SWITCHING (lease message) (dynamic visibility) | Next redesign (11) | `ENG-02` dedicated Stop and explicit Restart. Stop cancels all run-owned jobs then terminates. Restart replaces the run from current saved profile data. |
| `CAP-04-ENG-07` | Switch primary A → B without letting failed or late B replace A | `Menu.engine[]` / `ChooseMoreEngine`; Ctrl+1..9 / Ctrl+0; `EngineManager.switchEngine` | Last committed primary is in-memory. Autoload-last is not this control. | FAILED snapshot; quarantine; stale token success/fail ignored. If captured A is gone, primary becomes empty rather than silent B promotion. Setup-mode board rejects switch. | Source: `EngineSwitchUiTrackerTest.java:18-123`; `EngineManagerLifecycleReservationTest.java:81-113`; `Menu.java:5621-5627,6932-6968,9884-10074`; JAVA_BASELINE `#367` | Live key-repeat while SWITCHING (failure) | Next redesign (11) | Split: `ENG-03` transactional promotion; `ENG-04` failed switch and stale-identity rejection. |
| `CAP-04-ENG-08` | Report unexpected exit and recover without promoting stale identity | No dedicated button. 5 s timer `autoCheckEngineAlive` → `checkEngineAlive`. Settings `chkCheckEngineAlive` toggles live. Process exit / SSH disconnect / OpenCL native exit. | `auto-check-engine-alive` default **true**. Persist that ui key. Skipped if empty, setup mode, or engine-game occupying. | Java **automatically** restarts a dead primary (`restartEngineAutomatically` / remote restart). START_FAILED / NEEDS_REPAIR; click-to-repair. Engine-game participant recovery is 05. Next `ENG-07` waits for explicit Restart and does not auto-restart. | Source: `Config.java:1006,1719`; `EngineManager.java:685,1251-1269,3248-3272`; `ConfigDialog2.java:6104-6106`. Tests: `LeelazAutomaticRestartConvergenceTest` | Timer firing while the window is backgrounded (dynamic visibility). Remote-compute restart UX (`RCOMP-01`) (failure) | Next redesign (11) | `ENG-07` typed error, cancel jobs, wait for explicit Restart. No auto-restart. Do not migrate Swing diagnostic/quarantine presentation. |
| `CAP-04-ENG-09` | Preload non-primary saved engines in the background | Not a menu click. `MoreEngines` preload checkbox; EngineManager starts every non-selected `preload==true` | Default false, including bundled slot. Persist `engine-settings-list[].preload` | Secondary bundled startup must not publish primary CHECKING. Scheduling failure rethrows. Preloaded engines are not promoted to primary. | Source: `EngineData.java:9`; `EngineManager.java:101-119,632-710`; `Config.java:735-748`; `MoreEngines.java:528,595,823` | Whether a preloaded extra appears ready in the Engine menu before any switch (dynamic visibility) | Abandoned / absorbed redesign (11) | Exclusion — Abandoned as a background-preload product. Additional processes exist only inside a current transactional switch or domain-05 Match Reservation. |
| `CAP-04-ANA-01` | Run lightning/whole-game analysis on the loaded GTP engine instead of a dedicated analysis process | `AnalysisSettings.chkReuseCurrentEngine`; used by whole-game lease handoff | Default **false**. Persist `ui.analysis-reuse-current-engine` | Failed save leaves runtime flags and pending flash request unchanged. Exclusive lease blocks deferred HumanSL start. Empty primary → analysis unavailable. | Source: `Config.java:1037,1826`; `AnalysisSettings.java:86-88,492-527,644-721`; `WholeGameAnalysisSession.java:189-207` | Not required. | Absorbed redesign (11) | Absorbed redesign — `ENG-02`. Analysis jobs use the manager-owned Foreground Engine Run only when the adapter declares the required capability. Not a user preference. Game-only Generic GTP leaves unsupported analysis unavailable (`ENG-10`). |
| `CAP-04-ANA-02` | Keep a dedicated analysis process warm after app start | `AnalysisSettings.chkPreLoad`; `Lizzie.start` after EngineManager | Default **false**. Persist `ui.analysis-engine-preload` | Same save-failure atomicity as other analysis settings. Start failure is analysis-engine specific; primary stays up. | Source: `Config.java:1036,1825`; `Lizzie.java:1039-1041`; `AnalysisEngine.java:170-180,197-200` | Whether the dedicated process is visible in the Engine menu (source: separate from the catalog table) (dynamic visibility) | Absorbed redesign (11) | Absorbed redesign — `ENG-02`. Starting a capability-admitted Foreground Engine Run is the only analysis preload. No hidden second process setting. |
| `CAP-04-ANA-03` | Exit the dedicated analysis process when the current request finishes | `AnalysisSettings.chkAutoExit` | Default **true**. Persist `ui.analysis-auto-quit` | Save failure does not flip the flag. Process exit after job. | Source: `Config.java:1188,1993`; `AnalysisSettings.java:669-694`; `AnalysisEngineCommandHelper.java:21-22,68-70` | Not required. | Absorbed redesign (11) | Absorbed redesign — `ENG-02`. Jobs ending do not terminate the resident run. Only Stop, Restart, switch completion, application exit, or process failure ends it. |
| `CAP-04-PREF-LIZZIE-CACHE` | Reuse previously computed candidates/winrate on a node (Java in-tree cache) | Loaded from leelaz config | Default **true**. Persist `leelaz.enable-lizzie-cache`. **Not** Next SQLite | Incomplete payloads are not terminal success | Source: `Config.java:244,1855`; `BoardData.java:703-770` | Dedicated cache-toggle menu visibility (dynamic visibility). | Abandoned (11) | Exclusion — Abandoned product capability. Do not treat Java node payloads as `ANA-05`. Accepted SQLite cache remains the only runtime analysis cache. |
| `CAP-04-ANA-04` | Continuously analyze the current board with the foreground engine | Space `togglePonderMannul`; toolbar `analyse`; after engine ready; `AnalysisTable.stopGo`; Game › breakGame; FloatBoard Space. `Menu.pondering` is declared and **not added** — not an Entry Point. | Auto-ponder on ready: on. Toolbar `analyse` default true. Persist `ui.analyse`, time/playout limits, `show-ponder-limited-tips`. | Pause/restore via `ForegroundAnalysisPause` during switch/game start. Null `leelaz` no-op. Manual start syncs kifu first. Process stays up. | Source: `Lizzie.java:1250-1258`; `Menu.java:83`; `Input.java:419-421`; `LizzieFrame.java:12357-12367`; `BottomToolbar.java:81,226,1258-1264`; `MoveData.java:12-51`; `Config.java:64,1107-1110,1182,1884,1907-1909,2021`. Tests: `LizzieFrameRegressionTest.manualPonderStartSyncsCurrentKifuBeforeAnalysis`; `LeelazPonderStateTest` | Confirm `Menu.pondering` is never assigned after `doubleMenu` (none found through game-control construction). `playponder` persist key. | Deferred Next redesign (11) | `ANA-06` explicit current-node continuous analysis. Default-off, run-scoped, not persisted, never auto-starts on launch/ready/restart/switch. Next `ANA-01` is the supported selected-node path (Successor, not a Java row of its own except the Frozen-ID attachment below). |
| `CAP-04-ANA-05` | Analyze the selected node once | **Not a Java Entry Point.** Next: EngineSetupPanel 分析此手. Java closest surfaces are ponder / flash-part under `CAP-04-ANA-04` / `CAP-04-ANA-06`. | Next max_visits UI default 800; Java `analysis-max-visits` default 1. Persist profile `max_visits` + prefs `defaultMaxVisits` / Java ui key | Next: command error string; one-shot has no job-id cancel in the ENG-05 sense at the inventory baseline | Source: contrast `AnalysisEngine` vs Next `App.tsx:654-680`, `lib.rs:740-771`. Independent census: Java has no named one-shot JSONL job. | Not a Java Capability. Named so the Frozen ID remains for Ticket 11 mapping continuity. | Next redesign (11) | `ANA-01` through `ENG-05` selected-node lane, explicit cancellation, latest-request-wins, typed failure. |
| `CAP-04-ANA-06` | Fast overview analysis of game / part / all branches + settings | `FlashAnalyzeMenu` whole/part/all-branches/settings; `AutoAnalyzeMenu` lightning; Ctrl+B; right-click flash-this-move; silent load-flash settings are this Capability, not a new row | `analysis-max-visits` default 1. `auto-quick-analyze-on-load` default true. Toolbar `flash-analyze` defaults false when double menu + top toolbar, else true | Missing command → settings dialog; failed settings save does not start analysis. Cancel via `flashMode` / `tryStopFlashModeForManualGame`. | Source: `FlashAnalyzeMenu.java:11-46`; `AutoAnalyzeMenu.java:75-84`; `Config.java:1900-1901,1830-1832`; `RightClickMenu.java:185-191` | Whether load-flash runs on a file without a catalog engine (default) | Abandoned (11) | Exclusion — Abandoned product capability. One-shot (`ANA-01`) and mainline whole-game (`ANA-02`) are the supported Next workflows. |
| `CAP-04-ANA-07` | Two-stage baseline+deep analysis of the main line | `AutoAnalyzeMenu` 整盘精析 Shift+Ctrl+B | Baseline visits 32, minimum deep 500. Plan is not a file | `fail` engine / gameChanged; pause closes engine; resume fails if game identity changed | Source: `AutoAnalyzeMenu.java:14-72`; `WholeGameAnalysisSession.java:16-26,68-70,131-187,214-279`; `WholeGameAnalysisPlan.java:14-54` | Timeout copy when the analysis engine never becomes ready (failure) | Next redesign (11) | `ANA-02` single-stage, mainline-only whole-game session with progress and cancellation. Abandon Java baseline/deep weighting, pause/resume, retry-stage, and GTP lease handoff. |
| `CAP-04-ANA-08` | Auto-step analysis with start/end move, time/playouts, color filters | `AutoAnalyzeMenu` 自动分析 → `StartAnaDialog`; bottom toolbar; `Menu.stopAutoAnalyze`; `AnalysisTable` | start/end move default -1; anaBlack/anaWhite true; `analyzeAllBranch` false; `exitAutoAnalyzeByPause` true. Many `ui.auto-ana-*` keys | Dialog cancel resets auto-ana if cancelled | Source: `AutoAnalyzeMenu.java:26-54,99-104`; `StartAnaDialog.java:27-68`; `Config.java:941-950,1151-1163,1282-1283` | Not required. | Abandoned (11) | Exclusion — Abandoned product capability. Overlaps `ANA-01` / `ANA-02`. This abandonment is the later fact recorded against `SET-HINT-AUTOANALYZE` (Ticket 21). `SET-HINT-AUTOANALYZE` is excluded with this row. |
| `CAP-04-ANA-09` | Analyze a queue of SGF files | `AutoAnalyzeMenu` batch / batch-deep; `AnalysisTable` add files, stop/start | `batch-analysis-playouts` default 100. Persist that ui key + filesystem object | Destroy engine on stop in analysis-mode. Stop current / stop all. | Source: `AutoAnalyzeMenu.java:31-59`; `AnalysisTable.java:26-119`; `Config.java:1128,1621` | Queue progress while another engine occupies the primary (dynamic visibility) | Deferred Next redesign (11) | `ANA-07` session-only SGF file queue. Not restored after application restart. Cannot replace or dirty the current game. `APP-02` multi-file drop is the intake seam. |
| `CAP-04-ANA-10` | Single-stream tracking requests with immutable display state | Right-click `trackPoint` / `untrackPoint` / `clearAllTracked`; tracking UI interval + target visits | Default max visits 500. Persist tracking appearance + max visits | Lease unavailable; context mismatch. Clearing tracking resumes ordinary ponder. | Source: `TrackingAnalysisController.java:14-50`; `Config.java:1039-1045,1828-1829`; `RightClickMenu.java` track items | Not required. | Abandoned (11) | Exclusion — Abandoned as a domain-04 tracking job product. Do not reinterpret `UI-03` hover eviction as tracking. Readboard-origin behavior remains 06. |
| `CAP-04-ANA-11` | Render engine suggestions and heat/territory on the review surface | Always-on when analysis payload exists; View suggestion/estimate prefs; delay candidates; black/white candidates; Analyze › policy heatmap. Hawk-eye is `UI-05`, not this row. | `limit-max-suggestion` 10, `limit-branch-length` 15, `useMovesOwnership` true, estimate off, delay 10s off, show B/W candidates true | Occupied-point candidates stripped. Empty / failed engine → no overlay. | Source: `MoveData.java:12-35,52-110`; `AnalysisCandidateValidator.java:10-65`; `Config.java:199-200,1013-1016,1231-1235,1268,1852-1853` | Heatmap vs candidate interaction at runtime (ADR 0002 is Next law) (dynamic visibility) | Next redesign at existing Partial boundary (11) | `ANA-04` candidates, PV mini-board, ownership, policy, candidate-limit, bound to capability/run/job/node identity. `UI-03` remains frozen. Abandon Java black/white filtering, ten-second delay, PV-visits, branch-length, and other granular review display preferences. |
| `CAP-04-ANA-12` | Ensure later completions cannot publish onto a different engine/node/job | Implicit in switch, navigation, cancel, whole-game pause/cancel; flash/batch stop; ponder toggle off | N/A. Java generation / occupancy fences are machinery of this goal. | Ignore stale; rollback switch (recovered identity stays A); cancel session. Stale token / superseded completion must not publish on a different engine or node. | Source: `EngineStartupStatus.java:44-47`; `Lizzie.java:104-118`; `WholeGameAnalysisSession.java:90-97,155-181`; `AnalysisEngine.java:85-86`; `EngineSwitchUiTrackerTest`; `MenuEngineSwitchUiStateTest`; JAVA_BASELINE `#326` | Live Ctrl+digit while SWITCHING (also `CAP-04-ENG-07`) (failure) | Next redesign (11) | `ANA-03` two independent lanes (selected-node vs whole-game). `ENG-05` selected-node job identity. Stop/Restart/switch/crash/exit cancel all jobs for the run. |
| `CAP-04-ANA-13` | Carry analysis headers/payloads through SGF comments/properties | Save/open SGF; `appendWinrateToComment` default true. Engine-produced comment **data** is this row. | Persist in SGF file | Placeholder 50% winrate + visits must not look like a complete curve. Save failure is Domain 03. | Source: `BoardData.java:49-50,712-721`; `Config.java:196,1797` | Not required for engine-produced data. Ticket 03 runtime-checks comment persistence. | Deferred Next redesign (11) | `ANA-08` SGF analysis exchange. `SGF-05` comment separation unchanged. SQLite (`ANA-05`) stays separate; no live bidirectional sync. Ticket 20 Abandoned stuffing generated winrate/score/playouts into personal `C`; that write is not `ANA-08`. **Ticket 03** owns personal `C` mutation/persistence and `EXPORT-01`/`EXPORT-02`; this row only links those items. |
| `CAP-04-ANA-14` | Load/save/delete cached review frames for a game (Next SQLite) | **Not a Java Entry Point.** Next: auto on open if `autoLoadCache`; auto after analysis if `autoSaveAnalysis`; `CacheStatusBadge` | autoLoad/autoSave true. Persist `analysis-cache.sqlite3` (browser localStorage prefix in preview) | Status error; incompatible payload message | Next evidence: `analysisCache.ts`; `lib.rs:701-736,1409-1502`; `App.tsx:1082-1193`. No Java SQLite analogue. | Not a Java Capability. Named so the Frozen ID remains for Ticket 11 mapping continuity. | Frozen Accepted boundary (11) | `ANA-05`. Java in-tree cache is `CAP-04-PREF-LIZZIE-CACHE` (Abandoned). SGF exchange is `ANA-08`. |
| `CAP-04-ANA-15` | Show start/protocol/JSONL failures without leaking secrets | Start failure status; overlay chip; `EngineFailedMessage`; analysis settings fromError; KataGo JSON `error`; menu FAILED / rolled-back; `existing_lease` | N/A. In-session status. | Click-to-repair; settings after missing command. `EngineFailedMessage` redacts secrets. Recovery is Restart / repair chip / LoadEngine, not Settings. | Source: `EngineFailedMessage.java:43-59`; `EngineStartupStatus.java:10-14,118-124`. Tests: `EngineStartupStatusTest`; `EngineSwitchUiTrackerTest` | First-session diagnostic suppression (also `CAP-04-ENG-05`) (dynamic visibility) | Next redesign (11) | `ENG-07` typed Next-native outcomes. Failed switch preserves A (`ENG-04`). Profile repair remains available in Engine Settings. |

---

## Domain 05 — Game modes / match sessions

Independent census: [research 05](../.scratch/migration-baseline-v1-capability-audit/research/05-game-mode-capabilities.md) against Java `7b4027531c2b26062d0bfc27a040cc550cfbea4d`. Prior coverage-audit census: [research 05](../.scratch/migration-coverage-audit/research/05-game-modes.md). Dispositions: [Ticket 12](../.scratch/migration-coverage-audit/issues/12-decide-game-mode-dispositions.md), with later `GAME-09`/`CONTRIB-01` split from Ticket 24 and `GAME-10` from Ticket 25.

**Computed Domain 05 Capabilities: 8.** Reference 8. Extra/missing rows: none. The count was computed from registered, user-reachable surfaces; 8 was compared afterward and was not a stop condition.

Domain 05 owns match-session Capabilities and shared start-parameter surfaces only. Runtime-check is this domain's dedicated column. `GM-HUMAN-GENMOVE` and `GM-HUMAN-ANA` remain two census rows even though Ticket 12 merged their product. Existing Missing `GAME-01`–`GAME-03` may be replaced because they are not accepted history. Readboard engine play-back is Domain 06 `CAP-06-READBOARD` remainder → Deferred `GAME-10`, not a Domain 05 census row.

| Frozen ID | Name | User-reachable Entry Points | Default / persistence | Failure / recovery | Frozen evidence | Runtime-check | Disposition | Mapping |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `GM-HUMAN-GENMOVE` | Human vs one engine using GTP genmove | 棋局 → 新对局 genmove (`N`); independent-board N/Enter; double-menu or combined-toolbar NewGame when `showDoubleMenuGameControl`; 人机续弈 as White/Black; Enter continue; double-menu or combined-toolbar pause/resign/stop while live | Komi fallback 7.5; `new-game-komi` load 7.5; `check-play-black` true; `check-continue-play` false; `playponder` field default true; `auto-save-played-game` true. Persist those ui/leelaz keys. Live flags `isPlayingAgainstLeelaz` / `isGamePaused` are session-only. | No engine → modal. Engine switch failure aborts apply. WebSocket advanced clock refused. Contribute active → refuse. HumanSL teardown defers start. Cancel restores ponder. Handicap uses `place_free_handicap` or `fixed_handicap` then first `genmove`. | `NewGameDialog.java:889-1001`; `LizzieFrame.java:3869-3984,11856-11915,18882-18885`; `Menu.java:2912-2976,7619-7704,8647-8691,9090-9121,9522-9526`; `Input.java:412-417,655-660`; `InputIndependentMainBoard.java:214-223`; `Config.java:64,1707-1711,1906`; `Leelaz.java:8536-8541` | Double-menu pause/resign/stop visibility and labels while `isPlayingAgainstLeelaz` under `showDoubleMenuGameControl` (dynamic visibility) | Merged Next redesign with `GM-HUMAN-ANA` (12) | One Human-vs-Engine Match Session: `GAME-01` ownership/start, `GAME-02` human-vs-engine play, `GAME-04` shared rules/Compute Budgets, `GAME-05` SGF/review handoff. Adapter, not a second product mode, obtains the engine move. Abandoned as product controls: legacy genmove/analysis selector, GTP-versus-WebSocket/raw timing surfaces, pure-net toggle, Swing play-mode overlay. Legacy match autosave absorbed by `SGF-06` + `APP-04`. Clocks / auto-resign remain deferred `GAME-07`. |
| `GM-HUMAN-ANA` | Human vs one engine; engine plays by analysis + auto-play of the best move | Alt+N (main and independent-board); menu 人机对局(分析模式); analyze-mode continue items; Alt+Enter. Engines with `noAnalyze` redirect start/continue to genmove. Same double-menu or combined-toolbar pause/resign/stop. | Komi 7.5. Resign fields persist `anaGameResign*`. Shares continue/color/komi/handicap keys with genmove. Apply requires at least one of time / playouts / first-playouts / pure-net. Session flag `isAnaPlayingAgainstLeelaz`. | `apply()` sets `isAnaPlayingAgainstLeelaz` true before checks, so no-engine or invalid AI-move settings can leave occupancy until Stop; dialog cancel in `showAnalyzeGameDialogAfterModeTransition` forces the flag false and restores ponder. Contribute block; HumanSL deferral; lease conflict. Stop shares `stopAiPlayingAndPolicy`. Pause toggles ponder / `isGamePaused`. | `NewAnaGameDialog.java:734-934`; `LizzieFrame.java:11802-11854,12495-12520`; `Menu.java:2924-3007,9096-9110`; `Input.java:412-415,655-657`; `InputIndependentMainBoard.java:214-219` | Analyze-mode continue items' live `noAnalyze` redirect vs remaining visible analyze labels (dynamic visibility) | Merged Next redesign with `GM-HUMAN-GENMOVE` (12) | Same resulting items as `GM-HUMAN-GENMOVE`. Live-analysis policy is default-off, session-only, role-based (`off` / `human-turn` / `engine-turn` / `both`), reuses `ANA-04`, and is not persisted. |
| `GM-PK-SESSION` | Two engines play one game or a batch, including stop/pause/resume/revise and generated game information | 棋局 → 新对局 engine game (Alt+E); `NewEngineGameDialog` OK; BottomToolbar `enginePkPanel`. Stop/pause/revise/intervention stay **this** Capability. PK board clicks → `onClickedForManul`. | Komi 7.5, handicap 0, batchLimit 1, maxMoves 450, autosave true, resign minMove 0 / consecutive 2 / winrate 10.0. Persist new-engine-game keys, resign, times, PK identity. Snapshot Idle / BatchActive is session-only. | Same-engine / invalid analysis limits / occupied lifecycle rejected before board mutation. StartFailed unless user cancel. GTP console blocked. `Input` `VK_R` is `replayBranch`; `Alt+T` toggles comment — PK stop/pause are menu/chrome, not those keys. | `EngineGameModule.java:36-58,526-540`; `EngineGameParsedStart.java:238-253`; `EngineGameResignPolicy.java:3-6`; `Menu.java:2927-3107`; `Input.java:86-96,317-321,626-636,744-749`; `BottomToolbar.java:233-282,1444` | `enginePkPanel` occupancy versus other BottomToolbar detail slots (dynamic visibility) | Split Next redesign / Deferred remainder (12) | `GAME-03` one engine-vs-engine game from empty or exactly admissible position, with pause/resume, Stop, double pass, max moves, per-side typed failure, and stale-result rejection. `GAME-06` defers batch count and live revision, SGF opening catalogs, sequential/random openings, color exchange, manual intervention, batch folders / durable per-game output, and batch winrate images. Generated session information never overwrites personal `C` (`GAME-05` / `SGF-05`). |
| `GM-HUMANSL` | Human vs HumanSL policy opponent (AI Coach) | 棋局 → 新对局 HumanSL; toolbar AI Coach; training bar Pass / Retry AI / Finish and review; keyboard P when live; board clicks while HumanSL live. Not in the double-menu NewGame popup. | Mode POST_GAME_REVIEW, opponent RANK, rank 3 dan, color RANDOM, moveTime 10s, handicap 0, komi 7.5, fromCurrent false. Dialog fields are not written to `uiConfig`. | URL-SGF live sync blocks start. Failed start rolls back StartBoardSnapshot. AI no-response → Retry AI. Exit is retryable. Contribute / PK / human-engine starts defer behind `deferUntilHumanSlExit`. | `NewHumanSlGameDialog.java:214-221,1701-1713`; `HumanSlTrainingConfig.java:62-71`; `HumanSlGameController.java:32-80,292-433,490-767`; `Menu.java:2940-2949`; `Input.java:32-45,431-436`; `LizzieFrame.java:11689-11736` | `fromCurrent` checkbox enabled only when `moveNumber > 0` (dynamic visibility) | Deferred Next redesign (12) | `GAME-08` independent Match Session after `GAME-01`/`GAME-02`/`GAME-05` and a HumanSL-compatible profile. Not a hidden option of `GAME-02`. |
| `GM-MATCH-PASS` | Pass the current match turn | Keyboard P (HumanSL → `humanPass()`, else `board.pass()`); independent-board P always `board.pass()`; Menu 停一手 always `board.pass()`; double-menu pass; PK engines pass as `GameOutcome.DoublePass` | N/A | HumanSL ignores pass if finished or not human turn. Menu Pass during HumanSL is `board.pass()`, not `humanPass()`. Review pass remains Domain 03. | `Input.java:431-436`; `InputIndependentMainBoard.java:232-234`; `Menu.java:3141-3148,7161-7175,8207`; `HumanSlGameController.java:505-514`; `GameOutcome.java` | Not required | Split successor (12) | Match-scoped pass belongs to `GAME-02` / `GAME-03` and advances only through current game/run/job identity. Review-board pass remains `SGF-04` / claimed `UI-05` `P` with no match acceptance history. |
| `GM-MATCH-STOP` | End the current human-vs-engine (or HumanSL) match and return to review | Space / `Menu.breakGame` / `togglePonderMannul` (main and independent-board); double-menu or combined-toolbar Stop. HumanSL also: training-bar Finish. PK stop is `GM-PK-SESSION`. Resign writes `GameInfo` result then Stop. | `autoSavePlayedGame` gates Java MyGames timestamp autosave. Session flags are not persisted. | HumanSL abort is deferred/retryable. If no human match is active, Space falls through to ponder toggle. Stop does not fabricate `RE`; resign does write a result string before Stop. | `LizzieFrame.java:12357-12366,12417-12553`; `Menu.java:3034-3042,7619-7704,8628-8691,9522-9526`; `InputIndependentMainBoard.java:221-223` | Not required | Next redesign (12) | `GAME-01` / `GAME-02` Stop: cancel every session-owned job, release the reservation, preserve the last committed board, return to review. Does not double as a pondering toggle. Resign `RE` is `GAME-05`. Java autosave is absorbed by `SGF-06` + `APP-04`. |
| `GM-CONTRIBUTE` | Occupy the board with a KataGo contribute session and watch distributed games | 跑谱贡献 menu (visible iff `showContribute`); 可视化训练 → `startContributeEngine`; ContributeSettings `btnStart`; ContributeView first/prev/next/last/goto, Pause/Resume, slow/force stop, Save All, shutdown. Constructor sets `isContributing` after `startEngine()`. | Server `https://katagotraining.org/` unless overridden. Persist contribute* command/path/user/batch (02/06 cross-cut). `paused` is session-only. | SSH login failure still sets `isContributing` and clears the board after `startEngine()` returns. Pause sets `isContributing=false` while the process may still exist. Starting HumanSL/analyze/continue while contributing is refused. Drag on main board ignored while contributing. Benchmark-sync suppression returns before occupancy. | `ContributeEngine.java:70-126,519-541`; `ContributeView.java:61-193,492-543,787-791`; `ContributeSettings.java:461-472`; `Menu.java:5576-5610`; `Input.java:175`; `Config.java:2070` | ContributeView watch-index controls versus an empty `contributeGames` list (dynamic visibility) | Cross-domain Deferred redesign (12, 24) | Deferred `CONTRIB-01` owns the official `katagotraining.org` service, separate managed client, consent/credentials/privacy, narrow client-native network policy, one globally exclusive Contribution Run, typed bounded recovery/teardown, and optional completed-game auto-save (Ticket 06; not edited here). Deferred `GAME-09` owns only an explicitly opened transient main-board watcher and depends on Accepted `CONTRIB-01` plus `GAME-01`. Java custom service/command/SSH and granular watcher/Save-All controls are excluded. |
| `GM-MATCH-RULES-START` | Choose board size, komi, handicap, clocks, Compute-Budget-like limits, and PK max-moves at match start | All four start dialogs; 棋局 → 修改AI用时 `SetAiTimes`; GameInfo `I`; live `txtKomi`; PK max-move in EnginePkConfig. | Human/PK/HumanSL default komi 7.5, handicap 0. New game 19×19 via Domain 03/04 overlap. Persist family uiConfig keys. Live turn/pause/job/remaining time is session-only. | Illegal komi/handicap strings. WebSocket engines reject advanced clocks. PK occupied/same-engine rejected before mutation. | `NewGameDialog.java:889-977`; `GameInfo.java:13-20`; `EngineGameParsedStart.java:238-253`; `SetAiTimes.java:59-80,508-527`; `Menu.java:3021-3031,5824-5886`; `DesktopTimeControl`; `NewEngineGameDialog` handicap apply 961–1014 | Not required | Shared Next redesign (12) | `GAME-04` one game-level start contract: new-versus-current, board size, komi, handicap, side/profile assignments, per-side max visits and hard deadlines, PK max moves. New-game defaults 19×19, komi 7.5, handicap 0. Continuation inherits the exact current position and game-level rules. Human countdowns, main time/byoyomi/Fischer/engine-owned clocks, and configurable winrate auto-resign remain Deferred `GAME-07`. A Compute Budget never satisfies a Competitive Clock. |

---

## Domain 06 — Providers, synchronization, readboard, and external publishing

Independent census: [research 06](../.scratch/migration-baseline-v1-capability-audit/research/06-providers-sync-readboard-publishing.md) against Java `7b4027531c2b26062d0bfc27a040cc550cfbea4d`. Prior coverage-audit census: [research 06](../.scratch/migration-coverage-audit/research/06-providers-sync-readboard.md). Dispositions: [Ticket 13](../.scratch/migration-coverage-audit/issues/13-decide-provider-sync-readboard-dispositions.md), [Ticket 22](../.scratch/migration-coverage-audit/issues/22-decide-provider-network-proxy.md), [Ticket 24](../.scratch/migration-coverage-audit/issues/24-decide-contribute-service.md), [Ticket 25](../.scratch/migration-coverage-audit/issues/25-decide-readboard-gma.md), and [Ticket 26](../.scratch/migration-coverage-audit/issues/26-decide-yike-personal-auth-reads.md). `PROV-01`/`PROV-02`/`READ-01`/`READ-02` remain Partial at existing identity; `READ-03` stays Accepted; external engine play-back is Deferred `GAME-10`; Personal discovery and authenticated Yike read/play are Deferred `PROV-06`/`PROV-07`. This ticket owns the `CONTRIB-01` Matrix contract; Domain 05 `GM-CONTRIBUTE` remains the occupancy row. Preview never mutates the current game. One-shot import and ongoing sync are distinct. Successful trees enter through `SGF-07`. Provider, readboard, and Match sessions are not recovered by `APP-04`. `PROV-01`–`PROV-07` Next-owned remote traffic follows Provider Network Policy; readboard, browser-owned traffic, inbound WebBoard, and Contribution Network Policy do not.

**Computed Domain 06 Capabilities: 10.** Reference 10. Extra/missing rows: none. The count was computed from registered, user-reachable surfaces; 10 was compared afterward and was not a stop condition.

Runtime-check is this domain's dedicated column. Provider-live, sidecar-live, and credential evidence stay on Matrix rows.

| Frozen ID | Name | User-reachable Entry Points | Default / persistence | Failure / recovery | Frozen evidence | Runtime-check | Disposition | Mapping |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `CAP-06-ONLINE-URL` | Paste an online room/live URL and synchronize it into the current game | File → Open online link; shortcut Q without Ctrl/Alt; Yike Live Center Sync/double-click/Enter → `syncOnline` | `txtRefreshTime` UI default `"1"`; `proc()` uses that value when `> 0`, else 10. `alwaysGotoLastOnLive` default false; `openHtmlOnLive` default true. Persist those ui keys. No URL credential store. | Opening the dialog and applying a URL starts ongoing synchronization via `proc()`; Java has no separate non-mutating URL preview control. Pending Yike session promotes only when sync-ready and geometry-ready. Waiting-page URLs invalidate placement geometry without clearing the display session. Yike HTTP timeout 10s. Tencent/huanle live handshake bytes are Deferred `PROV-05`, not this locator set. | Source: `Menu.java:212-222`; `Input.java:701-707`; `OnlineDialog.java:137-156,348-358,834-836,3717-3724`; `YikeUrlParser.java:8-129`; `YikeLiveDialog.java:284-293`; `YikeApiClient.java:26-35` | Not required. Tencent/huanle live start from a pasted URL is Deferred `PROV-05` provider-live, not a Domain 06 Java runtime check. | Split redesign / Deferred (13) | Native provider center accepts recognized Yike locators without rendering the page (new/old live, old live board, game/hall room, unite room). Preview/Import → `PROV-01`. Explicit Start sync → `PROV-03`. Tencent/huanle live URLs → deferred `PROV-05`, not Yike or Tencent kifu import. |
| `CAP-06-YIKE-LIVE-CENTER` | Browse Yike live games, then start sync | Sync → 弈客直播 (Shift+O); BottomToolbar 直播 popup; Input Shift+O; InputIndependentMainBoard Shift+O | Dialog 920×420; first category Recommend; HTTP timeout 10_000 ms. Categories recommend `"1"`, local `"0"`, personal `"4"`. Guest `usertoken=-1`. No persist for last category/page. Compiled-in AppKey/signing headers exist; values are not listed. | loadFailed status; buttons disabled while loading. All categories use the same guest signed GET; no retry loop in `fetchLiveList`. Status must be 1200. | Source: `Menu.java:4134-4144`; `Input.java:532-538`; `BottomToolbar.java:1050-1059`; `YikeLiveDialog.java:41-163,231-293`; `YikeApiClient.java:26-76` | Not required. Meaningful guest Personal `official=4` results are `PROV-06` provider-live. | Split Next redesign / Deferred (13, 26) | Public `recommend`/`local` preview/import → `PROV-01`; public Start sync and Play & Sync → `PROV-03`. Frozen Personal-category discovery/handoff → Deferred `PROV-06`. Java proves no Yike login, private-room type, or native move API; formally authorized live-room read and provider-authoritative human play are separate Deferred Next redesign `PROV-07` with no frozen implementation evidence. |
| `CAP-06-YIKE-WEB` | Open Yike in the embedded browser (optional auto-sync) | Sync → 打开弈客网页版; Live Center Web; BottomToolbar same item | JCEF bundle required. `openHtmlOnLive` default true. Observed-URL freshness 30s | BrowserFrame startFailed; Retry or Open in system browser. | Source: `Menu.java:4146-4155`; `BottomToolbar.java:1061-1068`; `BrowserFrame.java:42-80` | Not required. Observed-URL auto-sync is JCEF implementation of Abandoned embedding. | Split redesign / Abandoned (13) | Abandon embedded browser hosting. Preserve website play through `PROV-03` Play & Sync (system browser + matching Next read-only sync). Stopping Next sync does not close the browser. |
| `CAP-06-YIKE-HALL` | Open Yike hall in the embedded browser | Sync → 弈客大厅; BottomToolbar 弈客大厅 | Fixed hall URL `https://home.yikeweiqi.com/#/game`. Persistence N/A. Hall parses as game room; unite as unite room. | Same JCEF failure path as `CAP-06-YIKE-WEB` | Source: `Menu.java:4157-4168`; `BottomToolbar.java:1071-1081`; `YikeApiClient.YIKE_GAME_URL`; `YikeUrlParser.java:101-125` | Not required | Split redesign / Abandoned (13) | Abandon embedded hall hosting. Hall and game-room URLs remain import/sync locators under `PROV-01` / `PROV-03`. |
| `CAP-06-FOX-KIFU` | Search Fox games by nickname/UID and import a selected kifu | Sync → 野狐(腾讯)棋谱; BottomToolbar live popup; top `btnFoxKifu` when `showBasicBtn` (default true) | Connect 20s, read 25s, 3 retries with 350ms×attempt. `foxAfterGet` default 0. Persist `last-fox-name`, `fox-after-get`, `fox-recent-searches` (up to 8, no passwords) | Empty list message; CGI then H5 fallback; stale `GetFoxRequest` results ignored (`source != foxReq`) | Source: `GetFoxRequest.java:27-40,150-266`; `FoxKifuDownload.java:470-478,849-877`; `Menu.java:4171-4180,8177`; `BottomToolbar.java:1084-1092` | Not required | Equivalent migration (13) | `PROV-02` nickname / UID / `chessid` preview, pagination, bounded recents, one-shot import through `SGF-07`. Not live synchronization. Not combined with Tencent. |
| `CAP-06-TENCENT-KIFU` | Search Tencent games and import a selected kifu | Sync → Tencent kifu; top `btnTencentKifu`. Not in BottomToolbar live popup | Connect 20s, read 25s, 3 retries with 350ms×attempt. Session token default `lizzieyzy-next`. Fetch num 100; UI 25/tab. Persist `tencent-recent-searches` (up to 8), `last-tencent-name`, `tencent-after-get` (load fallback `foxAfterGet`, default 0) | Same retry/timeout class as Fox; emitError on exception | Source: `Menu.java:4182-4191,8178`; `GetTencentRequest.java:17-29,150-186`; `TencentKifuDownload.java:84-98,825-854` | Not required | Equivalent migration (13) | `PROV-04` username/`chessId` preview, pagination, bounded recents, one-shot `SGF-07` import. Separate from `PROV-02` and from deferred `PROV-05`. |
| `CAP-06-READBOARD` | Synchronize an external client board via local sidecar | Sync → 棋盘识别工具 **Windows menu only**, accelerator Alt+O; BottomToolbar 棋盘同步 Windows only; Input and InputIndependentMainBoard Alt+O are **not** OS-gated | `alwaysSyncBoardStat` true; `readBoardGetFocus` true; `notPlaySoundInSync` true; `usePipeReadBoard` false. Persist always-sync, get-focus, pipeline, suppress-notice. Process exit 1000ms / destroy 200ms; pending ACK 3000ms | Missing exe/bat; place failed; GMA unsupported without KataGo. Off-Windows Alt+O: `openBoardSync()` prints stderr and returns | Source: `Menu.java:4193-4205`; `BottomToolbar.java:1094-1102`; `Input.java:532-536`; `LizzieFrame.java:2958-2965`; `ReadBoard.java:43-131`; `Config.java:101,1636,1743,2001` | Not required | Split equivalent migration / Deferred (13, 25) | `READ-01` sidecar readiness. `READ-02` one-way external-authoritative ongoing sidecar sync (advancing the Partial snapshot/import contract). `READ-03` stays the frozen OCR-unsupported claim. Both legacy engine play-back modes map to Deferred `GAME-10` External-board Engine Match: Final-decision and Leading-candidate share one Match lifecycle, while `READ-02` remains synchronization only. |
| `CAP-06-WEBBOARD` | Publish the current board to a LAN web viewer (trial play) | Sync → WebBoard Start/Stop + Copy URL; BottomToolbar live popup Start/Stop + Force-exit trial | HTTP 9998 (try +0..9); WS 9999; max connections 20; bind 0.0.0.0; idle 5 min. Optional `web-board` object in config | Start returns false if no HTTP/WS port; WS fail stops HTTP; trial denied in_use / engine_busy; copy URL no-ops if not running | Source: `Menu.java:4208-4269`; `BottomToolbar.java:1105-1148`; `WebBoardManager.java:24-227`; `WebBoardServer.java:10-73`; `WebBoardHttpServer.java:16-70` | Not required | Deferred (13) | `PUB-01` LAN board publishing (start/stop/copy URL). Trial counters and internal trial mechanics are implementation-only and excluded from that item. |
| `CAP-06-SHARE-CURRENT` | Registered share shortcuts with no working action | `Ctrl+E` and `Alt+B` in `Input` and `InputIndependentMainBoard` call `shareSGF()` | N/A. No observable state is written | No feedback; the registered action returns. `shareSGF()` / `batchShareSGF()` bodies are commented | Source: `Input.java:317-319,663-666`; `InputIndependentMainBoard.java:122,461`; `LizzieFrame.java:11570-11578`; `Menu.java:4043-4047,4858-4860` | Not required | Abandoned (13) | Exclusion — Abandoned product capability (reachable no-op). Do not create empty Next share actions. Unregistered share menubar/toolbar/batch/private/public upload are **not** this Capability (see Exclusions). |
| `CAP-06-SYNC-SETTINGS` | Live/readboard preference toggles | Sync menu checkboxes: open HTML on live; always jump to last live move; readboard always-sync; readboard get-focus | `openHtmlOnLive=true`; `alwaysGotoLastOnLive=false`; `alwaysSyncBoardStat=true`; `readBoardGetFocus=true`. Persist those ui keys. Listeners do not `config.save()` | No independent setting-error channel. Persist-write failure is SC-01/SC-03 | Source: `Menu.java:4273-4324`; `Config.java:95-103,1743,2001` | Whether each toggle applies to an already-running sync or readboard session (default) | Route to owner items (13, 26) | Sync intervals, locators, recents, retries, jump-to-last, mute-during-sync, non-secret provider-account references, and readboard always-sync/focus belong to their `PROV-01`–`PROV-07` and `READ-02` behavior owners. `PREF-01` is the mechanism only; `PROV-07` secrets use the System Credential Store. |

## Domain 07 — Install, update, packaging, release, and platform

Independent census: [research 07](../.scratch/migration-baseline-v1-capability-audit/research/07-update-packaging-release-platform.md) against Java `7b4027531c2b26062d0bfc27a040cc550cfbea4d`. Prior coverage-audit census: [research 07](../.scratch/migration-coverage-audit/research/07-update-packaging-release.md). Dispositions: [Ticket 14](../.scratch/migration-coverage-audit/issues/14-decide-update-release-dispositions.md). No end-user Domain 07 Capability is Deferred by Ticket 14. `REL-01` is a maintainer gate, not an end-user Capability. Existing IDs `REL-01`–`REL-05` remain stable; the oversized updater claim splits into `REL-06`–`REL-10`. Canonical Artifact runtime, trust, state, update/handoff, and removal contracts live in Matrix `REL-04` and research 07; this inventory does not restate that acceptance table. This inventory does not claim production artifacts, credentials, feeds, or Installed Live Evidence already exist.

**Computed Domain 07 end-user Capabilities: 11.** Reference 13. Extra rows: none. Missing as Capabilities: `REL-C12` (absorbed; in-app links are `REL-C11`) and `REL-C13` (Domain 02 `SET-CLEAR-PERSONAL-HISTORY`). The count was computed from registered Help/startup/install surfaces; 13 was compared afterward and was not a stop condition. `REL-C12` and `REL-C13` remain headings in the completeness index so every prior coverage-audit ID still appears once.

Runtime-check is this domain's dedicated column. Packaged/installed association live evidence stays on Matrix `REL-04`.

| Frozen ID | Name | User-reachable Entry Points | Default / persistence | Failure / recovery | Frozen evidence | Runtime-check | Disposition | Mapping |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `REL-C01` | Install / unpack a release package and start the product | Download from goagent.top/download or GitHub Releases; Windows portable unzip + flavor EXE; Windows `installer.exe`; macOS DMG drag to Applications; Linux unzip + `chmod +x start-linux64.sh && ./start-linux64.sh`. `core-update.zip`, experimental packages, and TensorRT split archives are Entry Points of this goal / `REL-C06`, not new IDs. | Windows ordinary users: `.portable.zip`. Flavor by GPU in Java. Next Canonical Artifacts are owned by Matrix `REL-04`. | Wrong package / incomplete unzip / Gatekeeper: TROUBLESHOOTING. macOS: do not run from the mounted DMG. | Source: `docs/INSTALL.md`; `docs/PACKAGES.md`; `docs/TROUBLESHOOTING.md`; `scripts/package_windows_exe.sh`; `scripts/package_macos_dmg.sh` | Not required. Clean-machine installability is `REL-04` Installed Live Evidence. | Next redesign (14) | `REL-04` Platform Package Lifecycle. Admit Windows NSIS **and** portable, macOS DMG, and Linux AppImage independently. Extra CI formats (MSI/deb/rpm) are not Canonical Artifacts. |
| `REL-C02` | Resolve a writable work directory on first launch (portable vs installed) | Process start (`Lizzie.main` → `WorkDirectoryResolver.resolve`) | `-Dlizzie.work.dir` if set. Windows portable: walk ≤8 parents for `.lizzie-portable` → `{root}/user-data`. Else first ASCII-safe writable of `{PUBLIC}/Documents/LizzieYzyNext`, `{PUBLIC}/LizzieYzyNext`, `{PROGRAMDATA}/LizzieYzyNext`, else `{userHome}/.lizzieyzy-next`. Non-Windows: writable cwd else `~/.lizzieyzy-next` (legacy `.lizzieyzy-next-foxuid` migrates). | Resolver records diagnostics. Portable recovery backup `config.before-portable-recovery.txt`. Unwritable cwd may switch `user.dir`. Profile save failure → Domain 04 repair chip. | Source: `WorkDirectoryResolver.java:28-180,226-278`; `WorkDirectoryEnvironment.java:12-68`; `Lizzie.java:334-336,381-386,462-476,894-964` | Which Windows shared-dir candidate a typical machine actually uses (dynamic default) | Next redesign (14) | `REL-04` data-location contract. Installed variants use OS app-data. Windows portable uses package-local `user-data/`. Installed removal preserves app-data by default; deleting a portable directory removes colocated state. No Java configuration or work-directory migration. |
| `REL-C03` | On first run (or host change), detect bundled KataGo and write a usable engine profile | Startup when `firstTimeLoad` or hostname change. Interactive 一键设置 is Domain 04, not this Capability. | If no engine and not `autoload-empty`, `inspectLocalSetup` then `applyAutoSetup` when engine+configs+weight present; persist `first-time-load=false`. `without.engine` stays empty. | Auto-setup exception logged; no bundle → `noBundledEngine`; save fail → profile-save repair. User can open Domain 04 one-click setup. | Source: `Lizzie.java:275-336,381-428,758-773,894-964,522-615`; `KataGoAutoSetupHelper` | Not required. NVIDIA CUDA staging is Domain 04. | Next redesign, domain 04 edge (14) | Start in No-engine Mode (`ENG-02` / `UI-04`); never download an engine automatically. Explicit Engine Settings acquires signed backend/model (`REL-05`); Domain 04 creates/confirms the profile and owns engine failure presentation. |
| `REL-C04` | Discover packaged flavor and bundled runtimes (Java, KataGo, weights, JCEF, readboard) | Implicit at update-check planning and first-run setup | Windows: `lizzieyzy-next-installed-manifest.json` flavor, else `engines/katago/windows-x64/lizzieyzy-next-engine-backend.txt` (`opencl`/`nvidia`/`nvidia50-cuda`→nvidia/`cpu`→with-katago), else `appDir/engines` → with-katago, else `without.engine`. Override `-Dlizzie.update.flavor`. macOS default `with-katago`. Portable marker `.lizzie-portable`. Persist installed manifest after successful Windows apply. | Missing matching package → `NO_PACKAGE`. Missing bundled engine → `REL-C03` repair chip. | Source: `WindowsUpdatePaths.java:20-178`; `PlatformUpdateService.java:92-231`; `InstalledUpdateState.java:14-43`; `docs/PACKAGES.md` | Not required | Next redesign (14, 24) | `REL-05` signed installed manifest of `app-core` and acquired KataGo backend/default-model. Abandon Java GPU flavor, bundled JRE, and JCEF. A readboard component joins only if its domain accepts update behavior. Admitted `CONTRIB-01` may add its signed client through this seam; the Deferred component is not an earlier R11 exit. |
| `REL-C05` | Manually check whether a newer signed release exists | Help → 检查更新 always added (`Menu.java:5219-5222`) → `CheckUpdateDialog`. Opening the page does **not** use the network; Check starts `UpdateDiscovery`. | Channel `stable`; source `official`. Unset version `next-dev`. Persist `update-channel`, `update-source`. Beta forces GitHub and does not persist a non-GitHub source. Java compares `next-YYYY-MM-DD.N`; Next uses SemVer. Java HTTP via `NetworkProxy.openConnection`; Next OS proxy only. Single-flight `UpdateCheckCoordinator.inFlight`. | Stay on page with localized unpackaged / unsupported / no-update / no-package / fetch / invalid / generic failures. Close blocked while check in flight. Unpackaged = `unavailableBuild()`. No production startup auto-check. | Source: `Menu.java:5164-5222`; `CheckUpdateDialog.java`; `UpdateDiscovery.java`; `UpdateManifestClient.java`; `UpdateChannel.java`; `UpdateSource.java`; `UpdateVersion.java` | Not required | Equivalent migration, Next-native implementation (14) | `REL-03` Update Discovery and Offer. SemVer comparison; OS proxy; signed-envelope/schema validation; single-flight; stable default and beta; official/GitHub source policy. |
| `REL-C06` | After a Windows offer, download selected components, quit, apply via helper, restart | Offer dialog from successful `REL-C05`: 立即更新, pause/resume/cancel, View Release, close | Selected components = those newer than the installed manifest. Staging `{workDir}/update/staging/{tag}/`; helper `lizzieyzy-next-updater-helper.jar`. | See `REL-C08`. Shutdown only after successful helper launch. IOException on helper launch leaves the process running. Non-portable and app dir not writable → PowerShell `RunAs`. SHA mismatch/cancel do not apply. | Source: `WindowsUpdateDialog.java:28-356`; `WindowsUpdateService.java:13-190`; `WindowsUpdateApplier.java:23-238`; `ResumableDownloader.java:22-38` | Which `*.exe` `Files.list` returns first under `appRoot` when several flavor launchers exist (dynamic default) | Equivalent migration, Next-native implementation (14) | `REL-06` Windows Component Download and Apply. Covers NSIS and portable roots without changing user data. Close through `APP-03` only after successful helper launch. |
| `REL-C07` | After a non-Windows offer, download the matching signed package and open it / its folder | `PackageUpdateDialog`: 下载新版, pause/resume/cancel, View release | Download dir `~/Downloads` if that path is a directory, else `{workDir}/update/downloads` | Cancel keeps `.part` for resume. Network fallback official → GitHub. Desktop unsupported → IOException. Never overwrite the running tree. | Source: `PackageUpdateDialog.java:26-220`; `PlatformUpdateService.java:38-50,112-126`; `PackageUpdateAdapter.java:1-64` | Not required | Equivalent migration, Next-native implementation (14) | `REL-07` macOS/Linux Package Update Handoff for matching DMG/AppImage. |
| `REL-C08` | Survive a failed Windows apply without leaving a half-replaced install | Automatic inside `WindowsUpdateApplier.apply`; download failures stay in `REL-C06` | Backup dir `staging/backup-<timestamp>/`; not a user setting | Wait for main PID ≤30s. Current files move to backup; exception restores in reverse. Zip-slip blocked. Maintainer GitHub-release withdrawal is **not** this Capability. | Source: `WindowsUpdateApplier.java:39-82,210-238,266-283,305-318`; `WindowsUpdateDialog.java:335-355` | Failure presentation if the helper fails after the app has already quit (`REL-08` Installed Live Evidence; source cannot show post-quit UI) | Equivalent migration, Next-native implementation (14) | `REL-08` Update Failure, Rollback, and Recovery. Pre-apply failures leave the running install untouched. Incomplete restoration retains the backup and provides repair/reinstall. |
| `REL-C09` | Integrate the installed app with the host OS enough to launch and not be blocked | Installer Start Menu / desktop shortcuts; macOS Dock icon (`Lizzie.installApplicationIcon`); macOS Privacy & Security Open Anyway. Linux `.desktop` is not a first-class user control. Frozen tree has no file-associations packaging script. | Taskbar then Java 8 `setDockIconImage`. | Icon failures ignored/logged. Gatekeeper/SmartScreen: TROUBLESHOOTING. | Source: `Lizzie.java:360,783-861,1012-1017`; `docs/INSTALL.md`; `docs/TROUBLESHOOTING.md` | Not required. OS file association and per-artifact launch/blockers are `REL-04` Installed Live Evidence. | Next redesign / cross-domain route (14) | Domain 07: `REL-02` package trust plus `REL-04` install/upgrade/removal and packaging evidence needed by `APP-01`. `APP-01` still owns `.sgf`/`.gib` activation and current-game replacement; packaging must not duplicate that claim. |
| `REL-C10` | Turn diagnostic logging on/off, export a sanitized zip, open the log folder | Help → Diagnostics and Logs; Help → Stop Full Trace (enabled iff full-trace active); dialog Apply / Export / Cancel export / Open log folder. CrashHandlers installed at bootstrap. | Defaults: diagnostics **on**, all modules, all scopes (`LoggingSettings.defaults`). Persist `uiConfig.logging`. Full Logs requires confirm; default off. Logs `{workDir}/logs`; export default `{workDir}/diagnostics`. | Apply failure reverts UI from runtime. Export cancel via flag; size-bounded and sanitized. Open-folder failures swallowed. No automatic upload. | Source: `Menu.java:5170-5205`; `DiagnosticsDialog.java:56-134,375-464,955-1025`; `LoggingSettings.java:10-101`; `LoggingRuntime.java:64-67`; `DiagnosticBundleExporter.java`; `CrashHandlers.java:16-80` | Stop Full Trace enabled only while `fullTraceActive`; scopes lock during an active session (dynamic visibility) | Next redesign (14) | `REL-09` Diagnostics and Support Bundle. Bounded ordinary local logs on by default; full trace explicit and default off; sanitized cancellable export; open-folder access. No automatic upload. Swing dialog structure is not the claim. |
| `REL-C11` | See product name, release tag, short intro, and project links | Help → About → `openConfigDialog2(2)`; settings modern nav About; check-update page header shows `Lizzie.nextVersion`. Repo/Releases/Issues via `createAboutLinkButton`. QQ `299419120` is copy, not a control. | Packaged builds inject `LIZZIE_NEXT_VERSION` / `-Dlizzie.next.version`. Unset → `next-dev`. Maven `2.5.3` is **not** the user-facing Next tag. | Browse no-ops if Desktop unsupported. | Source: `Menu.java:5208-5216`; `ConfigDialog2.java:146-162,2331-2458,2670-2684`; `Lizzie.java:275-294,754-773` | Not required | Next redesign (14) | `REL-10` Product Identity and Support Routing. Show actual installed SemVer and channel plus Releases, Issues, and support links. Abandon Java marketing-card presentation and the static `0.1.0` status string as evidence. |

Prior coverage-audit headings that this census does **not** count as Domain 07 Capabilities (Ticket 14 mappings kept; no new Parity IDs):

| Frozen ID | Why it is not a Domain 07 Capability | Mapping |
| --- | --- | --- |
| `REL-C12` | `SUPPORT.md`, issue templates, Discussions, and QQ-in-docs are maintainer/docs. In-app links are Entry Points of `REL-C11`. | Absorbed (14). Acceptance surfaces of `REL-09` and `REL-10`. |
| `REL-C13` | Help → `clearAllPersonalData` is already Domain 02 `SET-CLEAR-PERSONAL-HISTORY`. | Route to 03/06 (14): `SGF-09` and provider/search owners. Domain 07 does not create a broad personal-data reset. |

---

## Cross-domain routes (preserved, not duplicated)

These Entry Points are indexed in domain 01 research and owned by later domains. The inventory does not create a second Capability for the same user goal. Each physical surface has one canonical row; Frozen IDs named in Routing owner are census or link labels, not a second semantic owner.

| Surface | Routing owner | Semantic owner / mapping |
| --- | --- | --- |
| Native file activation / argv / drop of one kifu | 01 `SHELL-03` / `SHELL-12` | `APP-01` / `APP-02` → `SGF-07` (`SGF-08` for GIB) |
| Multi-file drop | 01 `SHELL-12` | `ANA-07` intake; never silent current-game replace |
| Autoload / engine picker / repair chip | 01 `SHELL-02` process-start radios / `SHELL-07` | 04 `CAP-04-ENG-02` → `ENG-06`; picker/repair `ENG-02` / `ENG-07`. Radios are not a second Domain 01 Capability |
| Window close / File › Exit | 01 `SHELL-08` | `APP-03`; persist mechanism `PREF-01`; engine/sidecar teardown 04/06/07 |
| N / 新建 | 01 routing | Next empty document `SGF-10`; Java genmove start is `GM-HUMAN-GENMOVE` → `GAME-02` |
| Space | 01 routing | Match Stop `GAME-01`/`GAME-02`; ponder is `ANA-06` (Deferred), not the same control |
| P | 01 routing | Review pass `SGF-04`; match pass `GAME-02`/`GAME-03` |
| Help › About / Check Update / Diagnostics / Clear personal data | 01 chrome / 07 Help; About also 02 dialog chrome and `REL-C11` | `REL-10` / `REL-03` / `REL-09`; `SET-CLEAR-PERSONAL-HISTORY` split 03/06 |
| Coordinates / move numbers | 02 census → 03 | `REVIEW-07` |
| Suggestion / Kata overlay prefs | 02 census → 04 | `ANA-04` (supported subset) |
| Contribute entry and actions | 02 census → 05/06 | `CONTRIB-01` service controls after acceptance; `GAME-09` Open Watch / Close Watch after acceptance; no disabled stub or visibility preference |
| Network proxy | 02 census → 06/07 | `REL-03` owns update OS proxy. `PROV-01`–`PROV-07` each own Provider Network Policy for Next-owned remote-provider traffic; Java application-proxy UI/keys are Abandoned with no migration or independent item. |
| Remote engine execution / compute center | 04 → 06 | Deferred `SSH-01` user-managed SSH Engine Profiles / Deferred `RCOMP-01` Zhizi and Custom `ws/wss` providers |
| Default board size / Ctrl+I / 设置棋盘大小 | 02 `SET-BOARD-SIZE`; 03 `SGF-03-ADJ-META` size half | `SGF-10` / `GAME-04`; names/komi stay `SGF-13` |
| URL / provider / readboard; File › 打开在线链接 / `Q`; provider `loadFile` | 03 `SGF-03-ADJ-URL` / `SGF-03-ADJ-PROVIDER` (link); 06 fetch | 06 `CAP-06-ONLINE-URL` → `PROV-01` / `PROV-03` / deferred `PROV-05`; other 06 owners; install `SGF-07`. Not Domain 03 Capabilities |
| Bundled engine auto-setup | 07 `REL-C03` | `REL-05` acquire + 04 profile/failure |
| File-association packaging evidence | 07 `REL-C09` | Semantics remain `APP-01` |
| Match SGF handoff / generated info | 05 | `GAME-05` writes through 03 current-game owner; `SGF-05` personal `C` |
| `APP-04` recovery | 01 | Never restores 04 runs, 04 jobs, 05 matches, or 06 sessions |
| View 下一手 / `J` | 02 `SET-NEXT-MOVE`; 03 `SGF-03-ADJ-NEXT-HINT` | `ANA-10`. Settings census vs overlay census |

## Settings traceability (not a catch-all settings phase)

Ticket 09 narrowed `PREF-01` to the durable-preference **mechanism** and a categorized Preferences **surface**. Semantic settings move with the behavior they affect. Lifecycle-affecting engine preferences belong with domain 04 items (`ENG-06`, `ENG-09`, analysis presentation), not a future catch-all settings phase.

| Settings cluster | Inventory IDs | Travels with |
| --- | --- | --- |
| Durable file / Preferences surface | `SET-PERSIST-CONFIG`, `SET-CONFIG-DIALOG-DISPLAY`, `SET-FIRST-USE` | `PREF-01` |
| Window geometry | `SET-RESET-WINDOW-POS`, `SET-PERSIST-WINDOW` (window half) | `WINDOW-01` |
| Workspace proportions / rails | `SET-RESTORE-PANEL-SIZES`, `SET-PERSIST-WINDOW` (proportion half), `SET-BOARD-POS`, `SET-LAYOUT-PANELS` | `LAYOUT-01`–`LAYOUT-04` |
| Appearance | `SET-THEME-BOARD-STYLE` | `APPEAR-01` |
| Localization | `SET-LANG` | `I18N-01` (Deferred) |
| Educational tips | `SET-RESET-HINTS` | `GUIDE-01` (Deferred, Ticket 21) |
| Board display booleans | `SET-COORDS`, `SET-MOVE-NUMBERS` | `REVIEW-07` |
| Review sound | `SET-SOUND` (play) | `REVIEW-08` |
| Sync mute / live toggles | `SET-SOUND` (mute), `CAP-06-SYNC-SETTINGS` | `PROV-01`–`PROV-07`, `READ-02` |
| Candidate / ownership / policy overlays | `SET-SUGGESTION-INFO`, `SET-KATA-DISPLAY` | `ANA-04` |
| Autoload | `SHELL-02`, `CAP-04-ENG-02` | `ENG-06` |
| Engine profile fields | `CAP-04-ENG-01` | `ENG-01` / `ENG-09` |
| Remote engine / compute settings | `CAP-04-ENG-01` SSH/remote remainder | `SSH-01` / `RCOMP-01` (Deferred, Ticket 23) |
| Match start defaults | `GM-MATCH-RULES-START`, `SET-BOARD-SIZE` (match half) | `GAME-04` |
| Update channel/source | `REL-C05` | `REL-03` |
| Update proxy | `SET-NETWORK-PROXY` (07 edge) | OS proxy via `REL-03` |
| Next-move Review Marker | `SET-NEXT-MOVE`, `SGF-03-ADJ-NEXT-HINT` | `ANA-10` via Ticket 17 |
| Winrate chart encoding | `SET-WINRATE-GRAPH` | `ANA-11` via Ticket 18 |
| Sub-Board Content Mode | `SET-SUBBOARD` | `ANA-12` via Ticket 19 |
| Player Names on Review | `SET-MAIN-PANEL` names extra | `REVIEW-09` via Ticket 20 |
| Main window always-on-top | `SET-MAIN-PANEL` always-on-top extra | `WINDOW-02` via Ticket 20 |

Abandoned or Swing-only settings (`SET-LOOKS`, `SET-FRAME-FONT`, `SET-THEME-APPLE-CLASSIC-CUSTOM`, `SET-THEME-DIALOG`, `SET-LAYOUT-MODE`, `SET-LAYOUT-TOOLBAR`, `SET-HINT-COMMENT-CTRL`) are exclusions, not a settings phase remainder.

## Exclusions

Three classes are distinct. Implementation-only and unregistered/dead surfaces are **not** Abandoned product Capabilities.

### Swing / implementation-only (not Abandoned)

These are widgets, threads, file formats, helper processes, or unregistered classes. They have no Parity Item and are not product abandonments.

| Surface | Why it is not a Capability | Recorded by |
| --- | --- | --- |
| Custom vs native `JMenuBar` / `WindowMenuStrip` | Menu-host mechanics (census `SHELL-10`) | Ticket 08 Swing-only |
| Java vs system look-and-feel | Swing L&F selection (census `SET-LOOKS`) | Ticket 09 Swing-only |
| Swing menus, dialogs, renderer hit-testing, Java config-file structure | Implementation details | Tickets 10–14 |
| `EngineStartupStatus` / switch tokens / generation fences / `AnalysisResourceCoordinator` / `ExactSnapshotEngineRestore` | Identity machinery | Research 04 |
| `EngineGameModule` chrome classes, `RetainedEngineModeTarget`, batch state objects | Implementation of `GM-PK-SESSION` | Research 05 |
| Java updater classes, Swing update controls, helper-process structure | Implementation of `REL-C05`–`REL-C08` | Ticket 14 |
| `DiagnosticsDialog` export/module/trace checkboxes as separate Capabilities; `DiagnosticBundleExporter` internals | Internals of `REL-C10` | Research 01/07 |
| `KifuLoadFinisher` 120 ms paint delay; `isSavingRaw` flags; `NodePath`/`generation`/`dirty` as user goals; `SgfObservation` logging | Widget / wire / diagnostics | Research 03 |
| WebBoard trial counters / internal trial mechanics | Excluded from `PUB-01` | Ticket 13 |
| Leftover `FoxRequest.jar` | Unused binary | Research 06 |
| Adapter/protocol enum names, JSON/GTP parser structure, thread topology, run-table representation | Implementation-only | Tickets 11–12 |
| `AppHealthDto` / `health` command | Internal Next API, not About | Research 07 |

### Unregistered / dead surfaces (not Abandoned product Capabilities)

Constructed objects that are never added, commented-out menu items, lab probes, and maintainer-only release mechanics. Reachable no-ops that Tickets 08–14 **did** classify as Abandoned product (`SHELL-04`, `SHELL-09`, `CAP-06-SHARE-CURRENT`) are **not** in this table.

| Surface | Why it is not an Entry Point | Recorded by |
| --- | --- | --- |
| `shareKifu` menubar (`this.add` commented) | Not added | Research 01/06 |
| Custom-toolbar share checkbox (`customToolbarItem.add(shareButton)` commented) | Not added | Research 06 |
| Batch/private/public share actions | Only behind unregistered Share menu / hidden toolbar | Research 06 / Ticket 13 |
| File › Resume previous game menu item | Commented `fileMenu.add(resume)`; startup `autoResume` is `SHELL-05` | Research 01/03 |
| `EditToolbar` methods commented; old `openConfigDialog()` commented | Dead code | Research 01 |
| `Discribe` helper dialog | No Help menu add | Research 01 |
| `-Dlizzie.smoke.open*` probes | Lab-only JVM flags | Research 01/07 |
| `Menu.intro` / Next 简介 | Next-only disabled stub; not in frozen Help | Research 01 |
| Sync diagnostics window | No menu/toolbar/shortcut registration found | Research 06 |
| Automatic update-check skip helper | No user Entry Point found | Research 07 |
| Capture Tsumego | Domain 03 puzzle, not provider sync | Research 06 |
| Commented KataGo score-perspective menu items (`Menu.java:2103-2135`) | Not Entry Points | Research 02 |

### Abandoned product capabilities

These **were** user-reachable Capabilities. Tickets 08–14 chose not to migrate them. They are listed in the domain tables above and summarized here so they are not confused with implementation-only surfaces.

| Abandoned observable behavior | Census pointer (row remains in the domain table) | Surviving supported path (if any) |
| --- | --- | --- |
| `read` launch mode | SHELL-04 | No-engine review `UI-04` |
| Visible Force Exit | SHELL-09 | Contextual Exit anyway only inside `APP-03` after teardown timeout |
| Hostname-triggered persist/preference deletion | SET-FIRST-LAUNCH (hostname-wipe half) | `WINDOW-01` invalid-geometry reset only |
| Forced initialize-settings wizard | SET-FIRST-USE | `PREF-01` surface |
| Application font-size slider | SET-FRAME-FONT | System DPI |
| Apple/Morandi/custom board assets | SET-THEME-APPLE-CLASSIC-CUSTOM | `APPEAR-01` curated pair |
| Separate theme editor | SET-THEME-DIALOG | `APPEAR-01` in Preferences |
| ExtraMode / classic / custom layouts | SET-LAYOUT-MODE | One adaptive workspace |
| Toolbar wrap/order/visibility prefs | SET-LAYOUT-TOOLBAR | Fixed Next toolbar |
| Comment-control instructional popup | SET-HINT-COMMENT-CTRL | — |
| Auto-analyze pause-exit educational tip | SET-HINT-AUTOANALYZE | — (`CAP-04-ANA-08` abandoned; `GUIDE-01` stays Deferred) |
| Lossy Swing raw / raw-with-comment saves | SGF-03-ADJ-SAVE-MORE (raw subset) | `SGF-05` comment separation; Save remains `SGF-06` |
| Sub-Board Image Export | SGF-03-ADJ-SAVE-MORE (sub-board image subset) | — |
| Thumbnail slots / second autosave-on-exit | SGF-03-ADJ-TEMP (manual slots) | `APP-04` |
| Conditional ignore of file `KM` | SGF-03-ADJ-KOMI (ignore-file-komi pref) | Root `KM` authoritative |
| Java 200 ms + delay dialog / manual reveal | SGF-03-ADJ-HOVER-DELAY | `UI-03` 120 ms |
| Last-engine autoload | CAP-04-ENG-03 | `ENG-06` Autoload Default |
| Background preload of extra GTP engines | CAP-04-ENG-09 | Switch / Match Reservation processes only |
| Java in-tree cache toggle | CAP-04-PREF-LIZZIE-CACHE | `ANA-05` SQLite |
| Lightning / part / all-branches flash | CAP-04-ANA-06 | `ANA-01` / `ANA-02` |
| Automatic current-game analysis | CAP-04-ANA-08 | `ANA-01` / `ANA-02`; `SET-HINT-AUTOANALYZE` excluded with this row (Ticket 21) |
| Mutually exclusive large-sub / large-WR enlarge presets | SET-MAIN-PANEL (large extras) | `LAYOUT-01` / `LAYOUT-04` adaptive workspace; `UI-01` chart/mini-board presence |
| Write generated winrate/score/playouts into personal `C` | SET-MAIN-PANEL (append-WR extra) | `SGF-05` personal comments; analysis pane display; deferred `ANA-08` structured headers |
| Tracking analysis jobs | CAP-04-ANA-10 | `UI-03` hover eviction unchanged; readboard stays 06 |
| Legacy genmove/analysis selector, raw timing, pure-net, play-mode overlay | GM-HUMAN-GENMOVE / GM-HUMAN-ANA (controls, not extra census IDs) | Adapter capability + `GAME-02` |
| JCEF embedded Yike page/hall | CAP-06-YIKE-WEB / CAP-06-YIKE-HALL (embedded hosting half) | `PROV-03` Play & Sync; locators remain |
| Reachable share shortcuts that do nothing | CAP-06-SHARE-CURRENT | No empty Next share actions |
| Flavor guessing, bundled JRE, JCEF as updater components | REL-C04 remainder | `REL-05` manifest of `app-core` + acquired KataGo components |

Deferred items (`I18N-01`, `GUIDE-01`, `EXPORT-01`, `EXPORT-02`, `EXPORT-03`, `SGF-15`, `SGF-16`, `REVIEW-04`–`REVIEW-06`, `ENG-08`, `SSH-01`, `CONTRIB-01`, `ANA-06`–`ANA-09`, `GAME-06`–`GAME-10`, `PROV-05`–`PROV-07`, `RCOMP-01`, `PUB-01`) are **not** Abandoned.

## Maintainer-only (not end-user Capabilities)

`REL-01` Release Preflight and Dry-run remains a matrix implementation gate (Ticket 14) and is **not** one of the 11 end-user `REL-C*` Capabilities. Java packaging scripts, Next `release.yml` / validators / dry-run workflows, signing scripts, and CI bundle formats other than the Canonical Artifacts are repository/release-environment evidence, not user Capabilities. Maintainer GitHub-release rollback cannot satisfy `REL-08`.

## Parity decision-gap closure after Tickets 17–28

Ticket 16 identified thirteen final owner-route or conflict records without supported Parity Item IDs from Tickets 08–14. Tickets 17–27 assigned the next-move, winrate-chart, Sub-Board mode, main-panel, guidance, provider-network, SSH/remote-compute, contribution, readboard GMA, Yike auth, and image-output remainders to supported IDs or explicit exclusions. Ticket 28 closes the final autoplay remainder through Missing R4 `ANA-13` Variation Replay and an explicit Engine Continuation exclusion.

All thirteen records now map exactly once to supported Parity Items or explicit exclusions. No parity decision gap remains.

## Corpus reconciliation (Ticket 08)

Independent reconstruction: [research 08](../.scratch/migration-baseline-v1-capability-audit/research/08-reconcile-inventory-routing.md). This file owns Entry Points, defaults, persistence, failure behavior, source references, and runtime-check status. Matrix statuses and Plan sequencing were not edited.

**Computed unique Java Capabilities: 135.** Reference 139. Extra Capability rows: none. Exact Frozen IDs that are not Capabilities: `CAP-04-ANA-05`, `CAP-04-ANA-14`, `REL-C12`, `REL-C13`. Unique Frozen IDs in domain tables: 139 (no duplicates). 139 was compared afterward and was not a stop condition.

Unresolved mapping remainder: **empty**. Duplicate Frozen IDs: **none**. Cross-domain routes do not create two semantic owners for one user goal. Each physical surface has one canonical Cross-domain row; foreign-owned and link-only Frozen IDs are named in that row.

Every Capability row has defaults, persistence, failure, source, and runtime-check status. Runtime checks name a dynamic-visibility, default, persistence, or failure fact, or are `Not required`.

## Census completeness index

Rebuilt from the computed Capability set. **135** Java Capabilities; **139** Frozen IDs. Each Frozen ID appears once in the domain lists below. Split keys inside a heading stay in that row.

### Domain 01 (13 Capabilities) — coverage-audit Ticket 08; recomputed by baseline-v1 Ticket 01 (computed 13, reference 13, mismatch none)

`SHELL-01` `SHELL-02` `SHELL-03` `SHELL-04` `SHELL-05` `SHELL-06` `SHELL-07` `SHELL-08` `SHELL-09` `SHELL-10` `SHELL-11` `SHELL-12` `SHELL-13`

### Domain 02 (35) — Ticket 09 (+ later owner IDs)

`SET-RESET-WINDOW-POS` `SET-RESTORE-PANEL-SIZES` `SET-RESET-HINTS` `SET-FIRST-LAUNCH` `SET-FIRST-USE` `SET-PERSIST-CONFIG` `SET-PERSIST-WINDOW` `SET-LANG` `SET-LOOKS` `SET-FRAME-FONT` `SET-SOUND` `SET-CONTRIBUTE-MENU-VIS` `SET-THEME-BOARD-STYLE` `SET-THEME-APPLE-CLASSIC-CUSTOM` `SET-THEME-DIALOG` `SET-CONFIG-DIALOG-DISPLAY` `SET-NETWORK-PROXY` `SET-BOARD-SIZE` `SET-LAYOUT-MODE` `SET-LAYOUT-PANELS` `SET-LAYOUT-TOOLBAR` `SET-BOARD-POS` `SET-COORDS` `SET-MOVE-NUMBERS` `SET-SUGGESTION-INFO` `SET-NEXT-MOVE` `SET-WINRATE-GRAPH` `SET-SUBBOARD` `SET-MAIN-PANEL` `SET-KATA-DISPLAY` `SET-HINT-NEWBOARD` `SET-HINT-REPLACE` `SET-HINT-COMMENT-CTRL` `SET-HINT-AUTOANALYZE` `SET-CLEAR-PERSONAL-HISTORY`

### Domain 03 (35 headings) — Ticket 10; recomputed by migration-baseline-v1-capability-audit Ticket 03 (computed 35, reference 35, mismatch none)

Accepted: `SGF-03-RT` `SGF-03-DTO` `SGF-03-NAV` `SGF-03-EDIT` `SGF-03-CMT` `SGF-03-SAVE` `SGF-03-RULE` `SGF-03-CHROME` `SGF-03-BOARD-INTENT` `SGF-03-HOVER` `SGF-03-NOENGINE` `SGF-03-ACTIONS`

Adjacent: `SGF-03-ADJ-OPEN` `SGF-03-ADJ-GIB` `SGF-03-ADJ-RECENT` `SGF-03-ADJ-CLIP` `SGF-03-ADJ-SAVE-MORE` `SGF-03-ADJ-TEMP` `SGF-03-ADJ-NEW` `SGF-03-ADJ-KOMI` `SGF-03-ADJ-SETUP` `SGF-03-ADJ-INSERT` `SGF-03-ADJ-DELETE-MOVE` `SGF-03-ADJ-MAIN` `SGF-03-ADJ-XFORM` `SGF-03-ADJ-META` `SGF-03-ADJ-MARKUP` `SGF-03-ADJ-AUTOPLAY` `SGF-03-ADJ-NEXT-HINT` `SGF-03-ADJ-TREE-CLICK` `SGF-03-ADJ-TRYPLAY / SCORE / LADDER` `SGF-03-ADJ-URL` `SGF-03-ADJ-PROVIDER` `SGF-03-ADJ-HOVER-DELAY` `SGF-03-ADJ-COMMENT-LAYERS`

### Domain 04 (23 Capabilities; 25 Frozen IDs) — Ticket 11; baseline-v1 Ticket 04 recomputed (computed 23, reference 25, extra none)

Capabilities: `CAP-04-ENG-01` `CAP-04-ENG-02` `CAP-04-ENG-03` `CAP-04-ENG-04` `CAP-04-ENG-05` `CAP-04-ENG-06` `CAP-04-ENG-07` `CAP-04-ENG-08` `CAP-04-ENG-09` `CAP-04-ANA-01` `CAP-04-ANA-02` `CAP-04-ANA-03` `CAP-04-PREF-LIZZIE-CACHE` `CAP-04-ANA-04` `CAP-04-ANA-06` `CAP-04-ANA-07` `CAP-04-ANA-08` `CAP-04-ANA-09` `CAP-04-ANA-10` `CAP-04-ANA-11` `CAP-04-ANA-12` `CAP-04-ANA-13` `CAP-04-ANA-15`

Not Java Capabilities (continuity Frozen IDs): `CAP-04-ANA-05` `CAP-04-ANA-14`

### Domain 05 (8) — Ticket 12; recomputed by migration-baseline-v1-capability-audit Ticket 05 (computed 8, reference 8, mismatch none)

`GM-HUMAN-GENMOVE` `GM-HUMAN-ANA` `GM-PK-SESSION` `GM-HUMANSL` `GM-MATCH-PASS` `GM-MATCH-STOP` `GM-CONTRIBUTE` `GM-MATCH-RULES-START`

### Domain 06 (10) — Ticket 13; recomputed by migration-baseline-v1-capability-audit Ticket 06 (computed 10, reference 10, mismatch none)

`CAP-06-ONLINE-URL` `CAP-06-YIKE-LIVE-CENTER` `CAP-06-YIKE-WEB` `CAP-06-YIKE-HALL` `CAP-06-FOX-KIFU` `CAP-06-TENCENT-KIFU` `CAP-06-READBOARD` `CAP-06-WEBBOARD` `CAP-06-SHARE-CURRENT` `CAP-06-SYNC-SETTINGS`

### Domain 07 (11 Capabilities; 13 headings) — Ticket 14; baseline-v1 Ticket 07 recomputed (computed 11, reference 13; two headings remain, not Capabilities)

Capabilities: `REL-C01` `REL-C02` `REL-C03` `REL-C04` `REL-C05` `REL-C06` `REL-C07` `REL-C08` `REL-C09` `REL-C10` `REL-C11`

Headings not counted: `REL-C12` `REL-C13`

## Sources

- Research: `.scratch/migration-coverage-audit/research/01-application-shell-entry-points.md` … `07-update-packaging-release.md`; independent domain recomputes: `.scratch/migration-baseline-v1-capability-audit/research/01-application-shell-interaction.md`, `02-settings-layout-persistence.md`, `03-sgf-board-review.md`, `04-audit-foreground-engine-analysis.md`, `05-game-mode-capabilities.md`, `06-providers-sync-readboard-publishing.md`, `07-update-packaging-release-platform.md`; corpus reconstruction: `.scratch/migration-baseline-v1-capability-audit/research/08-reconcile-inventory-routing.md`
- Dispositions: `.scratch/migration-coverage-audit/issues/08-decide-application-shell-dispositions.md` … `14-decide-update-release-dispositions.md`, plus remainder tickets `17`–`28`
- Standing R3 input used only as cited by Ticket 11: `.scratch/migration-coverage-audit/context/r3-foreground-engine-decisions.md`
- Item status, evidence, remaining gap, and acceptance remain in [`PARITY_MATRIX.md`](PARITY_MATRIX.md). Phases, dependencies, and exit criteria remain in [`MIGRATION_PLAN.md`](MIGRATION_PLAN.md).
