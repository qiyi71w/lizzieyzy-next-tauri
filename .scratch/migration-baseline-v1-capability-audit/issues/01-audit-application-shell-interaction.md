# 01 — Audit Application Shell and Interaction Capabilities

**What to build:** Produce one complete application-shell slice from frozen Java census through Next Disposition and roadmap placement. A user can trace ordinary launch, Native File Activation, file drop, Current-game Session Recovery, Safe Graceful Shutdown, shortcuts, help/reference access, and first-run behavior without duplicate SGF, layout, engine, or packaging ownership.

**Blocked by:** None — can start immediately.

**Status:** done

**Guardrails:** Adopt SGF/GIB cold/warm activation, one-file drop through safe replacement, multi-file routing to batch analysis, contextual unclean-exit recovery, and one Shortcut Registry/Reference. Abandon legacy `read`, hostname wiping, and permanent visible Force Exit; exclude Swing menu hosting. Cancelled or failed save aborts shutdown; Exit anyway appears only after bounded teardown failure. Do not expand Accepted items; new scope uses APP Successor Items.

- [x] Reconstruct shell Capabilities and registered UI, shortcut, pointer, OS, argv, drop, and dynamic Entry Points from Java revision `7b4027531c2b26062d0bfc27a040cc550cfbea4d` without using 13 as a stop condition; report the computed count, then compare 13 as reference evidence. A mismatch stays open with exact missing or extra rows.
- [x] Each Inventory row records Entry Points, observable defaults, persistence, failure or non-mutation behavior, source references, and runtime-check status. A runtime check is allowed only for a named dynamic-visibility, default, persistence, or failure fact not recoverable statically.
- [x] Every row maps to one supported Parity Item or explicit exclusion or absorbed redesign, including decision-complete APP-01 through APP-05 contracts.
- [x] Parsing and replacement route to SGF, batch and engine startup to foreground engine/analysis, geometry to settings/layout, and installed activation evidence to release/platform; this ticket records links only and does not edit foreign item contracts.
- [x] For every owned mapping, the Matrix owns status, evidence class, remaining gap, observable acceptance, and `Depends on`; the Plan owns phase membership and exits, Deferred Promotion Gates, Delivery Order, and Migration Phase Gates only.
- [x] Domain traceability has no application-shell remainder and preserves every original Accepted field against Next revision `18c6d189b8b01069975c4c40ead63a010249cb8c`.

## Answer

Independent census: [research 01](../research/01-application-shell-interaction.md). Destination: Domain 01 in `docs/JAVA_CAPABILITY_INVENTORY.md`, APP-01..APP-05 owned fields in `docs/PARITY_MATRIX.md`, and existing R5 / APP-01 split / R11 membership in `docs/MIGRATION_PLAN.md`.

- **Computed Domain 01 Capabilities: 13.** Reference 13. Extra/missing rows: none. Count was computed from registered, user-reachable surfaces; 13 was compared afterward.
- Each `SHELL-01`..`SHELL-13` row now records Entry Points, defaults, persistence, failure/non-mutation, source, and a dedicated Runtime-check column (`Not required` or a named permitted fact).
- Mappings stay Ticket 08 dispositions: APP-01..APP-05, UI-01/UI-04, ENG-06/ENG-02/ENG-07, REL-04/REL-05, WINDOW-01, plus Abandoned/Swing-only exclusions. No new IDs.
- Foreign contracts were not edited. GIB parsing remains linked as `SGF-08` through `SGF-07`. APP-01 Matrix `Depends on` is the Item Start Prerequisite `SGF-07` only; `REL-04` remains the Plan-owned R11 final-acceptance gate.
- No application-shell remainder. Original Accepted IDs, scopes, statuses, evidence, remaining gaps, and acceptance were not mutated by this ticket (`UI-02` remains Partial).
